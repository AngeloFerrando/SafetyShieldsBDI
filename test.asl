
@shield0("G(add_belief(perc_rad0(low)))", ["add_belief(perc_rad0(low))", "add_belief(perc_rad0(high))"])
+!inspect_nuclear_plant0 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant0 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield1("G(add_belief(perc_rad1(low)))", ["add_belief(perc_rad1(low))", "add_belief(perc_rad1(high))"])
+!inspect_nuclear_plant1 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant1 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield2("G(add_belief(perc_rad2(low)))", ["add_belief(perc_rad2(low))", "add_belief(perc_rad2(high))"])
+!inspect_nuclear_plant2 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant2 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield3("G(add_belief(perc_rad3(low)))", ["add_belief(perc_rad3(low))", "add_belief(perc_rad3(high))"])
+!inspect_nuclear_plant3 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant3 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield4("G(add_belief(perc_rad4(low)))", ["add_belief(perc_rad4(low))", "add_belief(perc_rad4(high))"])
+!inspect_nuclear_plant4 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant4 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield5("G(add_belief(perc_rad5(low)))", ["add_belief(perc_rad5(low))", "add_belief(perc_rad5(high))"])
+!inspect_nuclear_plant5 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant5 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield6("G(add_belief(perc_rad6(low)))", ["add_belief(perc_rad6(low))", "add_belief(perc_rad6(high))"])
+!inspect_nuclear_plant6 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant6 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield7("G(add_belief(perc_rad7(low)))", ["add_belief(perc_rad7(low))", "add_belief(perc_rad7(high))"])
+!inspect_nuclear_plant7 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant7 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield8("G(add_belief(perc_rad8(low)))", ["add_belief(perc_rad8(low))", "add_belief(perc_rad8(high))"])
+!inspect_nuclear_plant8 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant8 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield9("G(add_belief(perc_rad9(low)))", ["add_belief(perc_rad9(low))", "add_belief(perc_rad9(high))"])
+!inspect_nuclear_plant9 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant9 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield10("G(add_belief(perc_rad10(low)))", ["add_belief(perc_rad10(low))", "add_belief(perc_rad10(high))"])
+!inspect_nuclear_plant10 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant10 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield11("G(add_belief(perc_rad11(low)))", ["add_belief(perc_rad11(low))", "add_belief(perc_rad11(high))"])
+!inspect_nuclear_plant11 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant11 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield12("G(add_belief(perc_rad12(low)))", ["add_belief(perc_rad12(low))", "add_belief(perc_rad12(high))"])
+!inspect_nuclear_plant12 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant12 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield13("G(add_belief(perc_rad13(low)))", ["add_belief(perc_rad13(low))", "add_belief(perc_rad13(high))"])
+!inspect_nuclear_plant13 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant13 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield14("G(add_belief(perc_rad14(low)))", ["add_belief(perc_rad14(low))", "add_belief(perc_rad14(high))"])
+!inspect_nuclear_plant14 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant14 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield15("G(add_belief(perc_rad15(low)))", ["add_belief(perc_rad15(low))", "add_belief(perc_rad15(high))"])
+!inspect_nuclear_plant15 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant15 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield16("G(add_belief(perc_rad16(low)))", ["add_belief(perc_rad16(low))", "add_belief(perc_rad16(high))"])
+!inspect_nuclear_plant16 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant16 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield17("G(add_belief(perc_rad17(low)))", ["add_belief(perc_rad17(low))", "add_belief(perc_rad17(high))"])
+!inspect_nuclear_plant17 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant17 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield18("G(add_belief(perc_rad18(low)))", ["add_belief(perc_rad18(low))", "add_belief(perc_rad18(high))"])
+!inspect_nuclear_plant18 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant18 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield19("G(add_belief(perc_rad19(low)))", ["add_belief(perc_rad19(low))", "add_belief(perc_rad19(high))"])
+!inspect_nuclear_plant19 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant19 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield20("G(add_belief(perc_rad20(low)))", ["add_belief(perc_rad20(low))", "add_belief(perc_rad20(high))"])
+!inspect_nuclear_plant20 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant20 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield21("G(add_belief(perc_rad21(low)))", ["add_belief(perc_rad21(low))", "add_belief(perc_rad21(high))"])
+!inspect_nuclear_plant21 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant21 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield22("G(add_belief(perc_rad22(low)))", ["add_belief(perc_rad22(low))", "add_belief(perc_rad22(high))"])
+!inspect_nuclear_plant22 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant22 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield23("G(add_belief(perc_rad23(low)))", ["add_belief(perc_rad23(low))", "add_belief(perc_rad23(high))"])
+!inspect_nuclear_plant23 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant23 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield24("G(add_belief(perc_rad24(low)))", ["add_belief(perc_rad24(low))", "add_belief(perc_rad24(high))"])
+!inspect_nuclear_plant24 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant24 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield25("G(add_belief(perc_rad25(low)))", ["add_belief(perc_rad25(low))", "add_belief(perc_rad25(high))"])
+!inspect_nuclear_plant25 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant25 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield26("G(add_belief(perc_rad26(low)))", ["add_belief(perc_rad26(low))", "add_belief(perc_rad26(high))"])
+!inspect_nuclear_plant26 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant26 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield27("G(add_belief(perc_rad27(low)))", ["add_belief(perc_rad27(low))", "add_belief(perc_rad27(high))"])
+!inspect_nuclear_plant27 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant27 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield28("G(add_belief(perc_rad28(low)))", ["add_belief(perc_rad28(low))", "add_belief(perc_rad28(high))"])
+!inspect_nuclear_plant28 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant28 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield29("G(add_belief(perc_rad29(low)))", ["add_belief(perc_rad29(low))", "add_belief(perc_rad29(high))"])
+!inspect_nuclear_plant29 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant29 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield30("G(add_belief(perc_rad30(low)))", ["add_belief(perc_rad30(low))", "add_belief(perc_rad30(high))"])
+!inspect_nuclear_plant30 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant30 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield31("G(add_belief(perc_rad31(low)))", ["add_belief(perc_rad31(low))", "add_belief(perc_rad31(high))"])
+!inspect_nuclear_plant31 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant31 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield32("G(add_belief(perc_rad32(low)))", ["add_belief(perc_rad32(low))", "add_belief(perc_rad32(high))"])
+!inspect_nuclear_plant32 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant32 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield33("G(add_belief(perc_rad33(low)))", ["add_belief(perc_rad33(low))", "add_belief(perc_rad33(high))"])
+!inspect_nuclear_plant33 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant33 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield34("G(add_belief(perc_rad34(low)))", ["add_belief(perc_rad34(low))", "add_belief(perc_rad34(high))"])
+!inspect_nuclear_plant34 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant34 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield35("G(add_belief(perc_rad35(low)))", ["add_belief(perc_rad35(low))", "add_belief(perc_rad35(high))"])
+!inspect_nuclear_plant35 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant35 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield36("G(add_belief(perc_rad36(low)))", ["add_belief(perc_rad36(low))", "add_belief(perc_rad36(high))"])
+!inspect_nuclear_plant36 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant36 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield37("G(add_belief(perc_rad37(low)))", ["add_belief(perc_rad37(low))", "add_belief(perc_rad37(high))"])
+!inspect_nuclear_plant37 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant37 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield38("G(add_belief(perc_rad38(low)))", ["add_belief(perc_rad38(low))", "add_belief(perc_rad38(high))"])
+!inspect_nuclear_plant38 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant38 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield39("G(add_belief(perc_rad39(low)))", ["add_belief(perc_rad39(low))", "add_belief(perc_rad39(high))"])
+!inspect_nuclear_plant39 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant39 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield40("G(add_belief(perc_rad40(low)))", ["add_belief(perc_rad40(low))", "add_belief(perc_rad40(high))"])
+!inspect_nuclear_plant40 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant40 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield41("G(add_belief(perc_rad41(low)))", ["add_belief(perc_rad41(low))", "add_belief(perc_rad41(high))"])
+!inspect_nuclear_plant41 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant41 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield42("G(add_belief(perc_rad42(low)))", ["add_belief(perc_rad42(low))", "add_belief(perc_rad42(high))"])
+!inspect_nuclear_plant42 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant42 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield43("G(add_belief(perc_rad43(low)))", ["add_belief(perc_rad43(low))", "add_belief(perc_rad43(high))"])
+!inspect_nuclear_plant43 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant43 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield44("G(add_belief(perc_rad44(low)))", ["add_belief(perc_rad44(low))", "add_belief(perc_rad44(high))"])
+!inspect_nuclear_plant44 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant44 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield45("G(add_belief(perc_rad45(low)))", ["add_belief(perc_rad45(low))", "add_belief(perc_rad45(high))"])
+!inspect_nuclear_plant45 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant45 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield46("G(add_belief(perc_rad46(low)))", ["add_belief(perc_rad46(low))", "add_belief(perc_rad46(high))"])
+!inspect_nuclear_plant46 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant46 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield47("G(add_belief(perc_rad47(low)))", ["add_belief(perc_rad47(low))", "add_belief(perc_rad47(high))"])
+!inspect_nuclear_plant47 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant47 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield48("G(add_belief(perc_rad48(low)))", ["add_belief(perc_rad48(low))", "add_belief(perc_rad48(high))"])
+!inspect_nuclear_plant48 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant48 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield49("G(add_belief(perc_rad49(low)))", ["add_belief(perc_rad49(low))", "add_belief(perc_rad49(high))"])
+!inspect_nuclear_plant49 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant49 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield50("G(add_belief(perc_rad50(low)))", ["add_belief(perc_rad50(low))", "add_belief(perc_rad50(high))"])
+!inspect_nuclear_plant50 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant50 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield51("G(add_belief(perc_rad51(low)))", ["add_belief(perc_rad51(low))", "add_belief(perc_rad51(high))"])
+!inspect_nuclear_plant51 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant51 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield52("G(add_belief(perc_rad52(low)))", ["add_belief(perc_rad52(low))", "add_belief(perc_rad52(high))"])
+!inspect_nuclear_plant52 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant52 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield53("G(add_belief(perc_rad53(low)))", ["add_belief(perc_rad53(low))", "add_belief(perc_rad53(high))"])
+!inspect_nuclear_plant53 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant53 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield54("G(add_belief(perc_rad54(low)))", ["add_belief(perc_rad54(low))", "add_belief(perc_rad54(high))"])
+!inspect_nuclear_plant54 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant54 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield55("G(add_belief(perc_rad55(low)))", ["add_belief(perc_rad55(low))", "add_belief(perc_rad55(high))"])
+!inspect_nuclear_plant55 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant55 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield56("G(add_belief(perc_rad56(low)))", ["add_belief(perc_rad56(low))", "add_belief(perc_rad56(high))"])
+!inspect_nuclear_plant56 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant56 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield57("G(add_belief(perc_rad57(low)))", ["add_belief(perc_rad57(low))", "add_belief(perc_rad57(high))"])
+!inspect_nuclear_plant57 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant57 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield58("G(add_belief(perc_rad58(low)))", ["add_belief(perc_rad58(low))", "add_belief(perc_rad58(high))"])
+!inspect_nuclear_plant58 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant58 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield59("G(add_belief(perc_rad59(low)))", ["add_belief(perc_rad59(low))", "add_belief(perc_rad59(high))"])
+!inspect_nuclear_plant59 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant59 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield60("G(add_belief(perc_rad60(low)))", ["add_belief(perc_rad60(low))", "add_belief(perc_rad60(high))"])
+!inspect_nuclear_plant60 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant60 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield61("G(add_belief(perc_rad61(low)))", ["add_belief(perc_rad61(low))", "add_belief(perc_rad61(high))"])
+!inspect_nuclear_plant61 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant61 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield62("G(add_belief(perc_rad62(low)))", ["add_belief(perc_rad62(low))", "add_belief(perc_rad62(high))"])
+!inspect_nuclear_plant62 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant62 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield63("G(add_belief(perc_rad63(low)))", ["add_belief(perc_rad63(low))", "add_belief(perc_rad63(high))"])
+!inspect_nuclear_plant63 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant63 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield64("G(add_belief(perc_rad64(low)))", ["add_belief(perc_rad64(low))", "add_belief(perc_rad64(high))"])
+!inspect_nuclear_plant64 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant64 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield65("G(add_belief(perc_rad65(low)))", ["add_belief(perc_rad65(low))", "add_belief(perc_rad65(high))"])
+!inspect_nuclear_plant65 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant65 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield66("G(add_belief(perc_rad66(low)))", ["add_belief(perc_rad66(low))", "add_belief(perc_rad66(high))"])
+!inspect_nuclear_plant66 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant66 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield67("G(add_belief(perc_rad67(low)))", ["add_belief(perc_rad67(low))", "add_belief(perc_rad67(high))"])
+!inspect_nuclear_plant67 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant67 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield68("G(add_belief(perc_rad68(low)))", ["add_belief(perc_rad68(low))", "add_belief(perc_rad68(high))"])
+!inspect_nuclear_plant68 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant68 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield69("G(add_belief(perc_rad69(low)))", ["add_belief(perc_rad69(low))", "add_belief(perc_rad69(high))"])
+!inspect_nuclear_plant69 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant69 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield70("G(add_belief(perc_rad70(low)))", ["add_belief(perc_rad70(low))", "add_belief(perc_rad70(high))"])
+!inspect_nuclear_plant70 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant70 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield71("G(add_belief(perc_rad71(low)))", ["add_belief(perc_rad71(low))", "add_belief(perc_rad71(high))"])
+!inspect_nuclear_plant71 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant71 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield72("G(add_belief(perc_rad72(low)))", ["add_belief(perc_rad72(low))", "add_belief(perc_rad72(high))"])
+!inspect_nuclear_plant72 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant72 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield73("G(add_belief(perc_rad73(low)))", ["add_belief(perc_rad73(low))", "add_belief(perc_rad73(high))"])
+!inspect_nuclear_plant73 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant73 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield74("G(add_belief(perc_rad74(low)))", ["add_belief(perc_rad74(low))", "add_belief(perc_rad74(high))"])
+!inspect_nuclear_plant74 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant74 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield75("G(add_belief(perc_rad75(low)))", ["add_belief(perc_rad75(low))", "add_belief(perc_rad75(high))"])
+!inspect_nuclear_plant75 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant75 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield76("G(add_belief(perc_rad76(low)))", ["add_belief(perc_rad76(low))", "add_belief(perc_rad76(high))"])
+!inspect_nuclear_plant76 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant76 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield77("G(add_belief(perc_rad77(low)))", ["add_belief(perc_rad77(low))", "add_belief(perc_rad77(high))"])
+!inspect_nuclear_plant77 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant77 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield78("G(add_belief(perc_rad78(low)))", ["add_belief(perc_rad78(low))", "add_belief(perc_rad78(high))"])
+!inspect_nuclear_plant78 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant78 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield79("G(add_belief(perc_rad79(low)))", ["add_belief(perc_rad79(low))", "add_belief(perc_rad79(high))"])
+!inspect_nuclear_plant79 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant79 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield80("G(add_belief(perc_rad80(low)))", ["add_belief(perc_rad80(low))", "add_belief(perc_rad80(high))"])
+!inspect_nuclear_plant80 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant80 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield81("G(add_belief(perc_rad81(low)))", ["add_belief(perc_rad81(low))", "add_belief(perc_rad81(high))"])
+!inspect_nuclear_plant81 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant81 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield82("G(add_belief(perc_rad82(low)))", ["add_belief(perc_rad82(low))", "add_belief(perc_rad82(high))"])
+!inspect_nuclear_plant82 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant82 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield83("G(add_belief(perc_rad83(low)))", ["add_belief(perc_rad83(low))", "add_belief(perc_rad83(high))"])
+!inspect_nuclear_plant83 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant83 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield84("G(add_belief(perc_rad84(low)))", ["add_belief(perc_rad84(low))", "add_belief(perc_rad84(high))"])
+!inspect_nuclear_plant84 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant84 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield85("G(add_belief(perc_rad85(low)))", ["add_belief(perc_rad85(low))", "add_belief(perc_rad85(high))"])
+!inspect_nuclear_plant85 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant85 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield86("G(add_belief(perc_rad86(low)))", ["add_belief(perc_rad86(low))", "add_belief(perc_rad86(high))"])
+!inspect_nuclear_plant86 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant86 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield87("G(add_belief(perc_rad87(low)))", ["add_belief(perc_rad87(low))", "add_belief(perc_rad87(high))"])
+!inspect_nuclear_plant87 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant87 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield88("G(add_belief(perc_rad88(low)))", ["add_belief(perc_rad88(low))", "add_belief(perc_rad88(high))"])
+!inspect_nuclear_plant88 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant88 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield89("G(add_belief(perc_rad89(low)))", ["add_belief(perc_rad89(low))", "add_belief(perc_rad89(high))"])
+!inspect_nuclear_plant89 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant89 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield90("G(add_belief(perc_rad90(low)))", ["add_belief(perc_rad90(low))", "add_belief(perc_rad90(high))"])
+!inspect_nuclear_plant90 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant90 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield91("G(add_belief(perc_rad91(low)))", ["add_belief(perc_rad91(low))", "add_belief(perc_rad91(high))"])
+!inspect_nuclear_plant91 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant91 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield92("G(add_belief(perc_rad92(low)))", ["add_belief(perc_rad92(low))", "add_belief(perc_rad92(high))"])
+!inspect_nuclear_plant92 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant92 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield93("G(add_belief(perc_rad93(low)))", ["add_belief(perc_rad93(low))", "add_belief(perc_rad93(high))"])
+!inspect_nuclear_plant93 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant93 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield94("G(add_belief(perc_rad94(low)))", ["add_belief(perc_rad94(low))", "add_belief(perc_rad94(high))"])
+!inspect_nuclear_plant94 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant94 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield95("G(add_belief(perc_rad95(low)))", ["add_belief(perc_rad95(low))", "add_belief(perc_rad95(high))"])
+!inspect_nuclear_plant95 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant95 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield96("G(add_belief(perc_rad96(low)))", ["add_belief(perc_rad96(low))", "add_belief(perc_rad96(high))"])
+!inspect_nuclear_plant96 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant96 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield97("G(add_belief(perc_rad97(low)))", ["add_belief(perc_rad97(low))", "add_belief(perc_rad97(high))"])
+!inspect_nuclear_plant97 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant97 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield98("G(add_belief(perc_rad98(low)))", ["add_belief(perc_rad98(low))", "add_belief(perc_rad98(high))"])
+!inspect_nuclear_plant98 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant98 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield99("G(add_belief(perc_rad99(low)))", ["add_belief(perc_rad99(low))", "add_belief(perc_rad99(high))"])
+!inspect_nuclear_plant99 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant99 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield100("G(add_belief(perc_rad100(low)))", ["add_belief(perc_rad100(low))", "add_belief(perc_rad100(high))"])
+!inspect_nuclear_plant100 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant100 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield101("G(add_belief(perc_rad101(low)))", ["add_belief(perc_rad101(low))", "add_belief(perc_rad101(high))"])
+!inspect_nuclear_plant101 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant101 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield102("G(add_belief(perc_rad102(low)))", ["add_belief(perc_rad102(low))", "add_belief(perc_rad102(high))"])
+!inspect_nuclear_plant102 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant102 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield103("G(add_belief(perc_rad103(low)))", ["add_belief(perc_rad103(low))", "add_belief(perc_rad103(high))"])
+!inspect_nuclear_plant103 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant103 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield104("G(add_belief(perc_rad104(low)))", ["add_belief(perc_rad104(low))", "add_belief(perc_rad104(high))"])
+!inspect_nuclear_plant104 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant104 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield105("G(add_belief(perc_rad105(low)))", ["add_belief(perc_rad105(low))", "add_belief(perc_rad105(high))"])
+!inspect_nuclear_plant105 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant105 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield106("G(add_belief(perc_rad106(low)))", ["add_belief(perc_rad106(low))", "add_belief(perc_rad106(high))"])
+!inspect_nuclear_plant106 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant106 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield107("G(add_belief(perc_rad107(low)))", ["add_belief(perc_rad107(low))", "add_belief(perc_rad107(high))"])
+!inspect_nuclear_plant107 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant107 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield108("G(add_belief(perc_rad108(low)))", ["add_belief(perc_rad108(low))", "add_belief(perc_rad108(high))"])
+!inspect_nuclear_plant108 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant108 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield109("G(add_belief(perc_rad109(low)))", ["add_belief(perc_rad109(low))", "add_belief(perc_rad109(high))"])
+!inspect_nuclear_plant109 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant109 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield110("G(add_belief(perc_rad110(low)))", ["add_belief(perc_rad110(low))", "add_belief(perc_rad110(high))"])
+!inspect_nuclear_plant110 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant110 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield111("G(add_belief(perc_rad111(low)))", ["add_belief(perc_rad111(low))", "add_belief(perc_rad111(high))"])
+!inspect_nuclear_plant111 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant111 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield112("G(add_belief(perc_rad112(low)))", ["add_belief(perc_rad112(low))", "add_belief(perc_rad112(high))"])
+!inspect_nuclear_plant112 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant112 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield113("G(add_belief(perc_rad113(low)))", ["add_belief(perc_rad113(low))", "add_belief(perc_rad113(high))"])
+!inspect_nuclear_plant113 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant113 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield114("G(add_belief(perc_rad114(low)))", ["add_belief(perc_rad114(low))", "add_belief(perc_rad114(high))"])
+!inspect_nuclear_plant114 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant114 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield115("G(add_belief(perc_rad115(low)))", ["add_belief(perc_rad115(low))", "add_belief(perc_rad115(high))"])
+!inspect_nuclear_plant115 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant115 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield116("G(add_belief(perc_rad116(low)))", ["add_belief(perc_rad116(low))", "add_belief(perc_rad116(high))"])
+!inspect_nuclear_plant116 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant116 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield117("G(add_belief(perc_rad117(low)))", ["add_belief(perc_rad117(low))", "add_belief(perc_rad117(high))"])
+!inspect_nuclear_plant117 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant117 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield118("G(add_belief(perc_rad118(low)))", ["add_belief(perc_rad118(low))", "add_belief(perc_rad118(high))"])
+!inspect_nuclear_plant118 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant118 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield119("G(add_belief(perc_rad119(low)))", ["add_belief(perc_rad119(low))", "add_belief(perc_rad119(high))"])
+!inspect_nuclear_plant119 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant119 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield120("G(add_belief(perc_rad120(low)))", ["add_belief(perc_rad120(low))", "add_belief(perc_rad120(high))"])
+!inspect_nuclear_plant120 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant120 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield121("G(add_belief(perc_rad121(low)))", ["add_belief(perc_rad121(low))", "add_belief(perc_rad121(high))"])
+!inspect_nuclear_plant121 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant121 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield122("G(add_belief(perc_rad122(low)))", ["add_belief(perc_rad122(low))", "add_belief(perc_rad122(high))"])
+!inspect_nuclear_plant122 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant122 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield123("G(add_belief(perc_rad123(low)))", ["add_belief(perc_rad123(low))", "add_belief(perc_rad123(high))"])
+!inspect_nuclear_plant123 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant123 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield124("G(add_belief(perc_rad124(low)))", ["add_belief(perc_rad124(low))", "add_belief(perc_rad124(high))"])
+!inspect_nuclear_plant124 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant124 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield125("G(add_belief(perc_rad125(low)))", ["add_belief(perc_rad125(low))", "add_belief(perc_rad125(high))"])
+!inspect_nuclear_plant125 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant125 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield126("G(add_belief(perc_rad126(low)))", ["add_belief(perc_rad126(low))", "add_belief(perc_rad126(high))"])
+!inspect_nuclear_plant126 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant126 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield127("G(add_belief(perc_rad127(low)))", ["add_belief(perc_rad127(low))", "add_belief(perc_rad127(high))"])
+!inspect_nuclear_plant127 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant127 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield128("G(add_belief(perc_rad128(low)))", ["add_belief(perc_rad128(low))", "add_belief(perc_rad128(high))"])
+!inspect_nuclear_plant128 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant128 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield129("G(add_belief(perc_rad129(low)))", ["add_belief(perc_rad129(low))", "add_belief(perc_rad129(high))"])
+!inspect_nuclear_plant129 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant129 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield130("G(add_belief(perc_rad130(low)))", ["add_belief(perc_rad130(low))", "add_belief(perc_rad130(high))"])
+!inspect_nuclear_plant130 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant130 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield131("G(add_belief(perc_rad131(low)))", ["add_belief(perc_rad131(low))", "add_belief(perc_rad131(high))"])
+!inspect_nuclear_plant131 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant131 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield132("G(add_belief(perc_rad132(low)))", ["add_belief(perc_rad132(low))", "add_belief(perc_rad132(high))"])
+!inspect_nuclear_plant132 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant132 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield133("G(add_belief(perc_rad133(low)))", ["add_belief(perc_rad133(low))", "add_belief(perc_rad133(high))"])
+!inspect_nuclear_plant133 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant133 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield134("G(add_belief(perc_rad134(low)))", ["add_belief(perc_rad134(low))", "add_belief(perc_rad134(high))"])
+!inspect_nuclear_plant134 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant134 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield135("G(add_belief(perc_rad135(low)))", ["add_belief(perc_rad135(low))", "add_belief(perc_rad135(high))"])
+!inspect_nuclear_plant135 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant135 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield136("G(add_belief(perc_rad136(low)))", ["add_belief(perc_rad136(low))", "add_belief(perc_rad136(high))"])
+!inspect_nuclear_plant136 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant136 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield137("G(add_belief(perc_rad137(low)))", ["add_belief(perc_rad137(low))", "add_belief(perc_rad137(high))"])
+!inspect_nuclear_plant137 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant137 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield138("G(add_belief(perc_rad138(low)))", ["add_belief(perc_rad138(low))", "add_belief(perc_rad138(high))"])
+!inspect_nuclear_plant138 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant138 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield139("G(add_belief(perc_rad139(low)))", ["add_belief(perc_rad139(low))", "add_belief(perc_rad139(high))"])
+!inspect_nuclear_plant139 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant139 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield140("G(add_belief(perc_rad140(low)))", ["add_belief(perc_rad140(low))", "add_belief(perc_rad140(high))"])
+!inspect_nuclear_plant140 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant140 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield141("G(add_belief(perc_rad141(low)))", ["add_belief(perc_rad141(low))", "add_belief(perc_rad141(high))"])
+!inspect_nuclear_plant141 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant141 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield142("G(add_belief(perc_rad142(low)))", ["add_belief(perc_rad142(low))", "add_belief(perc_rad142(high))"])
+!inspect_nuclear_plant142 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant142 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield143("G(add_belief(perc_rad143(low)))", ["add_belief(perc_rad143(low))", "add_belief(perc_rad143(high))"])
+!inspect_nuclear_plant143 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant143 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield144("G(add_belief(perc_rad144(low)))", ["add_belief(perc_rad144(low))", "add_belief(perc_rad144(high))"])
+!inspect_nuclear_plant144 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant144 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield145("G(add_belief(perc_rad145(low)))", ["add_belief(perc_rad145(low))", "add_belief(perc_rad145(high))"])
+!inspect_nuclear_plant145 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant145 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield146("G(add_belief(perc_rad146(low)))", ["add_belief(perc_rad146(low))", "add_belief(perc_rad146(high))"])
+!inspect_nuclear_plant146 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant146 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield147("G(add_belief(perc_rad147(low)))", ["add_belief(perc_rad147(low))", "add_belief(perc_rad147(high))"])
+!inspect_nuclear_plant147 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant147 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield148("G(add_belief(perc_rad148(low)))", ["add_belief(perc_rad148(low))", "add_belief(perc_rad148(high))"])
+!inspect_nuclear_plant148 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant148 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield149("G(add_belief(perc_rad149(low)))", ["add_belief(perc_rad149(low))", "add_belief(perc_rad149(high))"])
+!inspect_nuclear_plant149 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant149 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield150("G(add_belief(perc_rad150(low)))", ["add_belief(perc_rad150(low))", "add_belief(perc_rad150(high))"])
+!inspect_nuclear_plant150 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant150 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield151("G(add_belief(perc_rad151(low)))", ["add_belief(perc_rad151(low))", "add_belief(perc_rad151(high))"])
+!inspect_nuclear_plant151 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant151 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield152("G(add_belief(perc_rad152(low)))", ["add_belief(perc_rad152(low))", "add_belief(perc_rad152(high))"])
+!inspect_nuclear_plant152 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant152 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield153("G(add_belief(perc_rad153(low)))", ["add_belief(perc_rad153(low))", "add_belief(perc_rad153(high))"])
+!inspect_nuclear_plant153 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant153 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield154("G(add_belief(perc_rad154(low)))", ["add_belief(perc_rad154(low))", "add_belief(perc_rad154(high))"])
+!inspect_nuclear_plant154 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant154 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield155("G(add_belief(perc_rad155(low)))", ["add_belief(perc_rad155(low))", "add_belief(perc_rad155(high))"])
+!inspect_nuclear_plant155 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant155 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield156("G(add_belief(perc_rad156(low)))", ["add_belief(perc_rad156(low))", "add_belief(perc_rad156(high))"])
+!inspect_nuclear_plant156 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant156 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield157("G(add_belief(perc_rad157(low)))", ["add_belief(perc_rad157(low))", "add_belief(perc_rad157(high))"])
+!inspect_nuclear_plant157 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant157 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield158("G(add_belief(perc_rad158(low)))", ["add_belief(perc_rad158(low))", "add_belief(perc_rad158(high))"])
+!inspect_nuclear_plant158 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant158 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield159("G(add_belief(perc_rad159(low)))", ["add_belief(perc_rad159(low))", "add_belief(perc_rad159(high))"])
+!inspect_nuclear_plant159 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant159 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield160("G(add_belief(perc_rad160(low)))", ["add_belief(perc_rad160(low))", "add_belief(perc_rad160(high))"])
+!inspect_nuclear_plant160 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant160 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield161("G(add_belief(perc_rad161(low)))", ["add_belief(perc_rad161(low))", "add_belief(perc_rad161(high))"])
+!inspect_nuclear_plant161 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant161 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield162("G(add_belief(perc_rad162(low)))", ["add_belief(perc_rad162(low))", "add_belief(perc_rad162(high))"])
+!inspect_nuclear_plant162 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant162 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield163("G(add_belief(perc_rad163(low)))", ["add_belief(perc_rad163(low))", "add_belief(perc_rad163(high))"])
+!inspect_nuclear_plant163 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant163 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield164("G(add_belief(perc_rad164(low)))", ["add_belief(perc_rad164(low))", "add_belief(perc_rad164(high))"])
+!inspect_nuclear_plant164 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant164 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield165("G(add_belief(perc_rad165(low)))", ["add_belief(perc_rad165(low))", "add_belief(perc_rad165(high))"])
+!inspect_nuclear_plant165 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant165 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield166("G(add_belief(perc_rad166(low)))", ["add_belief(perc_rad166(low))", "add_belief(perc_rad166(high))"])
+!inspect_nuclear_plant166 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant166 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield167("G(add_belief(perc_rad167(low)))", ["add_belief(perc_rad167(low))", "add_belief(perc_rad167(high))"])
+!inspect_nuclear_plant167 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant167 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield168("G(add_belief(perc_rad168(low)))", ["add_belief(perc_rad168(low))", "add_belief(perc_rad168(high))"])
+!inspect_nuclear_plant168 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant168 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield169("G(add_belief(perc_rad169(low)))", ["add_belief(perc_rad169(low))", "add_belief(perc_rad169(high))"])
+!inspect_nuclear_plant169 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant169 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield170("G(add_belief(perc_rad170(low)))", ["add_belief(perc_rad170(low))", "add_belief(perc_rad170(high))"])
+!inspect_nuclear_plant170 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant170 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield171("G(add_belief(perc_rad171(low)))", ["add_belief(perc_rad171(low))", "add_belief(perc_rad171(high))"])
+!inspect_nuclear_plant171 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant171 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield172("G(add_belief(perc_rad172(low)))", ["add_belief(perc_rad172(low))", "add_belief(perc_rad172(high))"])
+!inspect_nuclear_plant172 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant172 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield173("G(add_belief(perc_rad173(low)))", ["add_belief(perc_rad173(low))", "add_belief(perc_rad173(high))"])
+!inspect_nuclear_plant173 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant173 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield174("G(add_belief(perc_rad174(low)))", ["add_belief(perc_rad174(low))", "add_belief(perc_rad174(high))"])
+!inspect_nuclear_plant174 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant174 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield175("G(add_belief(perc_rad175(low)))", ["add_belief(perc_rad175(low))", "add_belief(perc_rad175(high))"])
+!inspect_nuclear_plant175 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant175 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield176("G(add_belief(perc_rad176(low)))", ["add_belief(perc_rad176(low))", "add_belief(perc_rad176(high))"])
+!inspect_nuclear_plant176 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant176 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield177("G(add_belief(perc_rad177(low)))", ["add_belief(perc_rad177(low))", "add_belief(perc_rad177(high))"])
+!inspect_nuclear_plant177 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant177 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield178("G(add_belief(perc_rad178(low)))", ["add_belief(perc_rad178(low))", "add_belief(perc_rad178(high))"])
+!inspect_nuclear_plant178 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant178 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield179("G(add_belief(perc_rad179(low)))", ["add_belief(perc_rad179(low))", "add_belief(perc_rad179(high))"])
+!inspect_nuclear_plant179 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant179 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield180("G(add_belief(perc_rad180(low)))", ["add_belief(perc_rad180(low))", "add_belief(perc_rad180(high))"])
+!inspect_nuclear_plant180 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant180 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield181("G(add_belief(perc_rad181(low)))", ["add_belief(perc_rad181(low))", "add_belief(perc_rad181(high))"])
+!inspect_nuclear_plant181 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant181 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield182("G(add_belief(perc_rad182(low)))", ["add_belief(perc_rad182(low))", "add_belief(perc_rad182(high))"])
+!inspect_nuclear_plant182 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant182 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield183("G(add_belief(perc_rad183(low)))", ["add_belief(perc_rad183(low))", "add_belief(perc_rad183(high))"])
+!inspect_nuclear_plant183 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant183 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield184("G(add_belief(perc_rad184(low)))", ["add_belief(perc_rad184(low))", "add_belief(perc_rad184(high))"])
+!inspect_nuclear_plant184 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant184 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield185("G(add_belief(perc_rad185(low)))", ["add_belief(perc_rad185(low))", "add_belief(perc_rad185(high))"])
+!inspect_nuclear_plant185 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant185 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield186("G(add_belief(perc_rad186(low)))", ["add_belief(perc_rad186(low))", "add_belief(perc_rad186(high))"])
+!inspect_nuclear_plant186 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant186 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield187("G(add_belief(perc_rad187(low)))", ["add_belief(perc_rad187(low))", "add_belief(perc_rad187(high))"])
+!inspect_nuclear_plant187 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant187 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield188("G(add_belief(perc_rad188(low)))", ["add_belief(perc_rad188(low))", "add_belief(perc_rad188(high))"])
+!inspect_nuclear_plant188 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant188 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield189("G(add_belief(perc_rad189(low)))", ["add_belief(perc_rad189(low))", "add_belief(perc_rad189(high))"])
+!inspect_nuclear_plant189 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant189 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield190("G(add_belief(perc_rad190(low)))", ["add_belief(perc_rad190(low))", "add_belief(perc_rad190(high))"])
+!inspect_nuclear_plant190 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant190 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield191("G(add_belief(perc_rad191(low)))", ["add_belief(perc_rad191(low))", "add_belief(perc_rad191(high))"])
+!inspect_nuclear_plant191 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant191 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield192("G(add_belief(perc_rad192(low)))", ["add_belief(perc_rad192(low))", "add_belief(perc_rad192(high))"])
+!inspect_nuclear_plant192 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant192 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield193("G(add_belief(perc_rad193(low)))", ["add_belief(perc_rad193(low))", "add_belief(perc_rad193(high))"])
+!inspect_nuclear_plant193 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant193 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield194("G(add_belief(perc_rad194(low)))", ["add_belief(perc_rad194(low))", "add_belief(perc_rad194(high))"])
+!inspect_nuclear_plant194 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant194 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield195("G(add_belief(perc_rad195(low)))", ["add_belief(perc_rad195(low))", "add_belief(perc_rad195(high))"])
+!inspect_nuclear_plant195 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant195 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield196("G(add_belief(perc_rad196(low)))", ["add_belief(perc_rad196(low))", "add_belief(perc_rad196(high))"])
+!inspect_nuclear_plant196 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant196 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield197("G(add_belief(perc_rad197(low)))", ["add_belief(perc_rad197(low))", "add_belief(perc_rad197(high))"])
+!inspect_nuclear_plant197 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant197 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield198("G(add_belief(perc_rad198(low)))", ["add_belief(perc_rad198(low))", "add_belief(perc_rad198(high))"])
+!inspect_nuclear_plant198 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant198 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield199("G(add_belief(perc_rad199(low)))", ["add_belief(perc_rad199(low))", "add_belief(perc_rad199(high))"])
+!inspect_nuclear_plant199 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant199 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield200("G(add_belief(perc_rad200(low)))", ["add_belief(perc_rad200(low))", "add_belief(perc_rad200(high))"])
+!inspect_nuclear_plant200 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant200 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield201("G(add_belief(perc_rad201(low)))", ["add_belief(perc_rad201(low))", "add_belief(perc_rad201(high))"])
+!inspect_nuclear_plant201 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant201 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield202("G(add_belief(perc_rad202(low)))", ["add_belief(perc_rad202(low))", "add_belief(perc_rad202(high))"])
+!inspect_nuclear_plant202 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant202 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield203("G(add_belief(perc_rad203(low)))", ["add_belief(perc_rad203(low))", "add_belief(perc_rad203(high))"])
+!inspect_nuclear_plant203 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant203 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield204("G(add_belief(perc_rad204(low)))", ["add_belief(perc_rad204(low))", "add_belief(perc_rad204(high))"])
+!inspect_nuclear_plant204 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant204 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield205("G(add_belief(perc_rad205(low)))", ["add_belief(perc_rad205(low))", "add_belief(perc_rad205(high))"])
+!inspect_nuclear_plant205 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant205 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield206("G(add_belief(perc_rad206(low)))", ["add_belief(perc_rad206(low))", "add_belief(perc_rad206(high))"])
+!inspect_nuclear_plant206 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant206 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield207("G(add_belief(perc_rad207(low)))", ["add_belief(perc_rad207(low))", "add_belief(perc_rad207(high))"])
+!inspect_nuclear_plant207 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant207 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield208("G(add_belief(perc_rad208(low)))", ["add_belief(perc_rad208(low))", "add_belief(perc_rad208(high))"])
+!inspect_nuclear_plant208 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant208 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield209("G(add_belief(perc_rad209(low)))", ["add_belief(perc_rad209(low))", "add_belief(perc_rad209(high))"])
+!inspect_nuclear_plant209 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant209 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield210("G(add_belief(perc_rad210(low)))", ["add_belief(perc_rad210(low))", "add_belief(perc_rad210(high))"])
+!inspect_nuclear_plant210 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant210 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield211("G(add_belief(perc_rad211(low)))", ["add_belief(perc_rad211(low))", "add_belief(perc_rad211(high))"])
+!inspect_nuclear_plant211 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant211 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield212("G(add_belief(perc_rad212(low)))", ["add_belief(perc_rad212(low))", "add_belief(perc_rad212(high))"])
+!inspect_nuclear_plant212 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant212 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield213("G(add_belief(perc_rad213(low)))", ["add_belief(perc_rad213(low))", "add_belief(perc_rad213(high))"])
+!inspect_nuclear_plant213 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant213 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield214("G(add_belief(perc_rad214(low)))", ["add_belief(perc_rad214(low))", "add_belief(perc_rad214(high))"])
+!inspect_nuclear_plant214 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant214 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield215("G(add_belief(perc_rad215(low)))", ["add_belief(perc_rad215(low))", "add_belief(perc_rad215(high))"])
+!inspect_nuclear_plant215 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant215 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield216("G(add_belief(perc_rad216(low)))", ["add_belief(perc_rad216(low))", "add_belief(perc_rad216(high))"])
+!inspect_nuclear_plant216 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant216 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield217("G(add_belief(perc_rad217(low)))", ["add_belief(perc_rad217(low))", "add_belief(perc_rad217(high))"])
+!inspect_nuclear_plant217 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant217 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield218("G(add_belief(perc_rad218(low)))", ["add_belief(perc_rad218(low))", "add_belief(perc_rad218(high))"])
+!inspect_nuclear_plant218 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant218 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield219("G(add_belief(perc_rad219(low)))", ["add_belief(perc_rad219(low))", "add_belief(perc_rad219(high))"])
+!inspect_nuclear_plant219 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant219 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield220("G(add_belief(perc_rad220(low)))", ["add_belief(perc_rad220(low))", "add_belief(perc_rad220(high))"])
+!inspect_nuclear_plant220 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant220 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield221("G(add_belief(perc_rad221(low)))", ["add_belief(perc_rad221(low))", "add_belief(perc_rad221(high))"])
+!inspect_nuclear_plant221 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant221 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield222("G(add_belief(perc_rad222(low)))", ["add_belief(perc_rad222(low))", "add_belief(perc_rad222(high))"])
+!inspect_nuclear_plant222 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant222 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield223("G(add_belief(perc_rad223(low)))", ["add_belief(perc_rad223(low))", "add_belief(perc_rad223(high))"])
+!inspect_nuclear_plant223 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant223 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield224("G(add_belief(perc_rad224(low)))", ["add_belief(perc_rad224(low))", "add_belief(perc_rad224(high))"])
+!inspect_nuclear_plant224 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant224 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield225("G(add_belief(perc_rad225(low)))", ["add_belief(perc_rad225(low))", "add_belief(perc_rad225(high))"])
+!inspect_nuclear_plant225 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant225 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield226("G(add_belief(perc_rad226(low)))", ["add_belief(perc_rad226(low))", "add_belief(perc_rad226(high))"])
+!inspect_nuclear_plant226 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant226 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield227("G(add_belief(perc_rad227(low)))", ["add_belief(perc_rad227(low))", "add_belief(perc_rad227(high))"])
+!inspect_nuclear_plant227 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant227 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield228("G(add_belief(perc_rad228(low)))", ["add_belief(perc_rad228(low))", "add_belief(perc_rad228(high))"])
+!inspect_nuclear_plant228 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant228 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield229("G(add_belief(perc_rad229(low)))", ["add_belief(perc_rad229(low))", "add_belief(perc_rad229(high))"])
+!inspect_nuclear_plant229 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant229 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield230("G(add_belief(perc_rad230(low)))", ["add_belief(perc_rad230(low))", "add_belief(perc_rad230(high))"])
+!inspect_nuclear_plant230 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant230 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield231("G(add_belief(perc_rad231(low)))", ["add_belief(perc_rad231(low))", "add_belief(perc_rad231(high))"])
+!inspect_nuclear_plant231 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant231 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield232("G(add_belief(perc_rad232(low)))", ["add_belief(perc_rad232(low))", "add_belief(perc_rad232(high))"])
+!inspect_nuclear_plant232 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant232 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield233("G(add_belief(perc_rad233(low)))", ["add_belief(perc_rad233(low))", "add_belief(perc_rad233(high))"])
+!inspect_nuclear_plant233 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant233 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield234("G(add_belief(perc_rad234(low)))", ["add_belief(perc_rad234(low))", "add_belief(perc_rad234(high))"])
+!inspect_nuclear_plant234 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant234 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield235("G(add_belief(perc_rad235(low)))", ["add_belief(perc_rad235(low))", "add_belief(perc_rad235(high))"])
+!inspect_nuclear_plant235 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant235 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield236("G(add_belief(perc_rad236(low)))", ["add_belief(perc_rad236(low))", "add_belief(perc_rad236(high))"])
+!inspect_nuclear_plant236 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant236 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield237("G(add_belief(perc_rad237(low)))", ["add_belief(perc_rad237(low))", "add_belief(perc_rad237(high))"])
+!inspect_nuclear_plant237 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant237 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield238("G(add_belief(perc_rad238(low)))", ["add_belief(perc_rad238(low))", "add_belief(perc_rad238(high))"])
+!inspect_nuclear_plant238 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant238 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield239("G(add_belief(perc_rad239(low)))", ["add_belief(perc_rad239(low))", "add_belief(perc_rad239(high))"])
+!inspect_nuclear_plant239 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant239 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield240("G(add_belief(perc_rad240(low)))", ["add_belief(perc_rad240(low))", "add_belief(perc_rad240(high))"])
+!inspect_nuclear_plant240 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant240 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield241("G(add_belief(perc_rad241(low)))", ["add_belief(perc_rad241(low))", "add_belief(perc_rad241(high))"])
+!inspect_nuclear_plant241 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant241 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield242("G(add_belief(perc_rad242(low)))", ["add_belief(perc_rad242(low))", "add_belief(perc_rad242(high))"])
+!inspect_nuclear_plant242 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant242 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield243("G(add_belief(perc_rad243(low)))", ["add_belief(perc_rad243(low))", "add_belief(perc_rad243(high))"])
+!inspect_nuclear_plant243 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant243 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield244("G(add_belief(perc_rad244(low)))", ["add_belief(perc_rad244(low))", "add_belief(perc_rad244(high))"])
+!inspect_nuclear_plant244 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant244 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield245("G(add_belief(perc_rad245(low)))", ["add_belief(perc_rad245(low))", "add_belief(perc_rad245(high))"])
+!inspect_nuclear_plant245 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant245 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield246("G(add_belief(perc_rad246(low)))", ["add_belief(perc_rad246(low))", "add_belief(perc_rad246(high))"])
+!inspect_nuclear_plant246 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant246 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield247("G(add_belief(perc_rad247(low)))", ["add_belief(perc_rad247(low))", "add_belief(perc_rad247(high))"])
+!inspect_nuclear_plant247 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant247 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield248("G(add_belief(perc_rad248(low)))", ["add_belief(perc_rad248(low))", "add_belief(perc_rad248(high))"])
+!inspect_nuclear_plant248 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant248 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield249("G(add_belief(perc_rad249(low)))", ["add_belief(perc_rad249(low))", "add_belief(perc_rad249(high))"])
+!inspect_nuclear_plant249 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant249 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield250("G(add_belief(perc_rad250(low)))", ["add_belief(perc_rad250(low))", "add_belief(perc_rad250(high))"])
+!inspect_nuclear_plant250 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant250 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield251("G(add_belief(perc_rad251(low)))", ["add_belief(perc_rad251(low))", "add_belief(perc_rad251(high))"])
+!inspect_nuclear_plant251 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant251 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield252("G(add_belief(perc_rad252(low)))", ["add_belief(perc_rad252(low))", "add_belief(perc_rad252(high))"])
+!inspect_nuclear_plant252 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant252 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield253("G(add_belief(perc_rad253(low)))", ["add_belief(perc_rad253(low))", "add_belief(perc_rad253(high))"])
+!inspect_nuclear_plant253 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant253 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield254("G(add_belief(perc_rad254(low)))", ["add_belief(perc_rad254(low))", "add_belief(perc_rad254(high))"])
+!inspect_nuclear_plant254 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant254 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield255("G(add_belief(perc_rad255(low)))", ["add_belief(perc_rad255(low))", "add_belief(perc_rad255(high))"])
+!inspect_nuclear_plant255 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant255 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield256("G(add_belief(perc_rad256(low)))", ["add_belief(perc_rad256(low))", "add_belief(perc_rad256(high))"])
+!inspect_nuclear_plant256 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant256 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield257("G(add_belief(perc_rad257(low)))", ["add_belief(perc_rad257(low))", "add_belief(perc_rad257(high))"])
+!inspect_nuclear_plant257 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant257 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield258("G(add_belief(perc_rad258(low)))", ["add_belief(perc_rad258(low))", "add_belief(perc_rad258(high))"])
+!inspect_nuclear_plant258 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant258 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield259("G(add_belief(perc_rad259(low)))", ["add_belief(perc_rad259(low))", "add_belief(perc_rad259(high))"])
+!inspect_nuclear_plant259 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant259 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield260("G(add_belief(perc_rad260(low)))", ["add_belief(perc_rad260(low))", "add_belief(perc_rad260(high))"])
+!inspect_nuclear_plant260 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant260 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield261("G(add_belief(perc_rad261(low)))", ["add_belief(perc_rad261(low))", "add_belief(perc_rad261(high))"])
+!inspect_nuclear_plant261 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant261 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield262("G(add_belief(perc_rad262(low)))", ["add_belief(perc_rad262(low))", "add_belief(perc_rad262(high))"])
+!inspect_nuclear_plant262 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant262 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield263("G(add_belief(perc_rad263(low)))", ["add_belief(perc_rad263(low))", "add_belief(perc_rad263(high))"])
+!inspect_nuclear_plant263 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant263 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield264("G(add_belief(perc_rad264(low)))", ["add_belief(perc_rad264(low))", "add_belief(perc_rad264(high))"])
+!inspect_nuclear_plant264 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant264 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield265("G(add_belief(perc_rad265(low)))", ["add_belief(perc_rad265(low))", "add_belief(perc_rad265(high))"])
+!inspect_nuclear_plant265 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant265 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield266("G(add_belief(perc_rad266(low)))", ["add_belief(perc_rad266(low))", "add_belief(perc_rad266(high))"])
+!inspect_nuclear_plant266 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant266 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield267("G(add_belief(perc_rad267(low)))", ["add_belief(perc_rad267(low))", "add_belief(perc_rad267(high))"])
+!inspect_nuclear_plant267 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant267 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield268("G(add_belief(perc_rad268(low)))", ["add_belief(perc_rad268(low))", "add_belief(perc_rad268(high))"])
+!inspect_nuclear_plant268 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant268 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield269("G(add_belief(perc_rad269(low)))", ["add_belief(perc_rad269(low))", "add_belief(perc_rad269(high))"])
+!inspect_nuclear_plant269 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant269 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield270("G(add_belief(perc_rad270(low)))", ["add_belief(perc_rad270(low))", "add_belief(perc_rad270(high))"])
+!inspect_nuclear_plant270 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant270 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield271("G(add_belief(perc_rad271(low)))", ["add_belief(perc_rad271(low))", "add_belief(perc_rad271(high))"])
+!inspect_nuclear_plant271 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant271 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield272("G(add_belief(perc_rad272(low)))", ["add_belief(perc_rad272(low))", "add_belief(perc_rad272(high))"])
+!inspect_nuclear_plant272 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant272 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield273("G(add_belief(perc_rad273(low)))", ["add_belief(perc_rad273(low))", "add_belief(perc_rad273(high))"])
+!inspect_nuclear_plant273 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant273 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield274("G(add_belief(perc_rad274(low)))", ["add_belief(perc_rad274(low))", "add_belief(perc_rad274(high))"])
+!inspect_nuclear_plant274 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant274 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield275("G(add_belief(perc_rad275(low)))", ["add_belief(perc_rad275(low))", "add_belief(perc_rad275(high))"])
+!inspect_nuclear_plant275 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant275 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield276("G(add_belief(perc_rad276(low)))", ["add_belief(perc_rad276(low))", "add_belief(perc_rad276(high))"])
+!inspect_nuclear_plant276 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant276 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield277("G(add_belief(perc_rad277(low)))", ["add_belief(perc_rad277(low))", "add_belief(perc_rad277(high))"])
+!inspect_nuclear_plant277 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant277 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield278("G(add_belief(perc_rad278(low)))", ["add_belief(perc_rad278(low))", "add_belief(perc_rad278(high))"])
+!inspect_nuclear_plant278 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant278 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield279("G(add_belief(perc_rad279(low)))", ["add_belief(perc_rad279(low))", "add_belief(perc_rad279(high))"])
+!inspect_nuclear_plant279 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant279 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield280("G(add_belief(perc_rad280(low)))", ["add_belief(perc_rad280(low))", "add_belief(perc_rad280(high))"])
+!inspect_nuclear_plant280 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant280 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield281("G(add_belief(perc_rad281(low)))", ["add_belief(perc_rad281(low))", "add_belief(perc_rad281(high))"])
+!inspect_nuclear_plant281 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant281 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield282("G(add_belief(perc_rad282(low)))", ["add_belief(perc_rad282(low))", "add_belief(perc_rad282(high))"])
+!inspect_nuclear_plant282 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant282 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield283("G(add_belief(perc_rad283(low)))", ["add_belief(perc_rad283(low))", "add_belief(perc_rad283(high))"])
+!inspect_nuclear_plant283 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant283 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield284("G(add_belief(perc_rad284(low)))", ["add_belief(perc_rad284(low))", "add_belief(perc_rad284(high))"])
+!inspect_nuclear_plant284 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant284 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield285("G(add_belief(perc_rad285(low)))", ["add_belief(perc_rad285(low))", "add_belief(perc_rad285(high))"])
+!inspect_nuclear_plant285 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant285 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield286("G(add_belief(perc_rad286(low)))", ["add_belief(perc_rad286(low))", "add_belief(perc_rad286(high))"])
+!inspect_nuclear_plant286 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant286 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield287("G(add_belief(perc_rad287(low)))", ["add_belief(perc_rad287(low))", "add_belief(perc_rad287(high))"])
+!inspect_nuclear_plant287 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant287 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield288("G(add_belief(perc_rad288(low)))", ["add_belief(perc_rad288(low))", "add_belief(perc_rad288(high))"])
+!inspect_nuclear_plant288 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant288 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield289("G(add_belief(perc_rad289(low)))", ["add_belief(perc_rad289(low))", "add_belief(perc_rad289(high))"])
+!inspect_nuclear_plant289 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant289 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield290("G(add_belief(perc_rad290(low)))", ["add_belief(perc_rad290(low))", "add_belief(perc_rad290(high))"])
+!inspect_nuclear_plant290 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant290 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield291("G(add_belief(perc_rad291(low)))", ["add_belief(perc_rad291(low))", "add_belief(perc_rad291(high))"])
+!inspect_nuclear_plant291 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant291 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield292("G(add_belief(perc_rad292(low)))", ["add_belief(perc_rad292(low))", "add_belief(perc_rad292(high))"])
+!inspect_nuclear_plant292 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant292 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield293("G(add_belief(perc_rad293(low)))", ["add_belief(perc_rad293(low))", "add_belief(perc_rad293(high))"])
+!inspect_nuclear_plant293 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant293 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield294("G(add_belief(perc_rad294(low)))", ["add_belief(perc_rad294(low))", "add_belief(perc_rad294(high))"])
+!inspect_nuclear_plant294 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant294 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield295("G(add_belief(perc_rad295(low)))", ["add_belief(perc_rad295(low))", "add_belief(perc_rad295(high))"])
+!inspect_nuclear_plant295 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant295 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield296("G(add_belief(perc_rad296(low)))", ["add_belief(perc_rad296(low))", "add_belief(perc_rad296(high))"])
+!inspect_nuclear_plant296 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant296 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield297("G(add_belief(perc_rad297(low)))", ["add_belief(perc_rad297(low))", "add_belief(perc_rad297(high))"])
+!inspect_nuclear_plant297 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant297 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield298("G(add_belief(perc_rad298(low)))", ["add_belief(perc_rad298(low))", "add_belief(perc_rad298(high))"])
+!inspect_nuclear_plant298 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant298 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield299("G(add_belief(perc_rad299(low)))", ["add_belief(perc_rad299(low))", "add_belief(perc_rad299(high))"])
+!inspect_nuclear_plant299 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant299 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield300("G(add_belief(perc_rad300(low)))", ["add_belief(perc_rad300(low))", "add_belief(perc_rad300(high))"])
+!inspect_nuclear_plant300 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant300 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield301("G(add_belief(perc_rad301(low)))", ["add_belief(perc_rad301(low))", "add_belief(perc_rad301(high))"])
+!inspect_nuclear_plant301 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant301 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield302("G(add_belief(perc_rad302(low)))", ["add_belief(perc_rad302(low))", "add_belief(perc_rad302(high))"])
+!inspect_nuclear_plant302 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant302 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield303("G(add_belief(perc_rad303(low)))", ["add_belief(perc_rad303(low))", "add_belief(perc_rad303(high))"])
+!inspect_nuclear_plant303 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant303 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield304("G(add_belief(perc_rad304(low)))", ["add_belief(perc_rad304(low))", "add_belief(perc_rad304(high))"])
+!inspect_nuclear_plant304 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant304 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield305("G(add_belief(perc_rad305(low)))", ["add_belief(perc_rad305(low))", "add_belief(perc_rad305(high))"])
+!inspect_nuclear_plant305 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant305 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield306("G(add_belief(perc_rad306(low)))", ["add_belief(perc_rad306(low))", "add_belief(perc_rad306(high))"])
+!inspect_nuclear_plant306 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant306 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield307("G(add_belief(perc_rad307(low)))", ["add_belief(perc_rad307(low))", "add_belief(perc_rad307(high))"])
+!inspect_nuclear_plant307 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant307 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield308("G(add_belief(perc_rad308(low)))", ["add_belief(perc_rad308(low))", "add_belief(perc_rad308(high))"])
+!inspect_nuclear_plant308 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant308 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield309("G(add_belief(perc_rad309(low)))", ["add_belief(perc_rad309(low))", "add_belief(perc_rad309(high))"])
+!inspect_nuclear_plant309 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant309 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield310("G(add_belief(perc_rad310(low)))", ["add_belief(perc_rad310(low))", "add_belief(perc_rad310(high))"])
+!inspect_nuclear_plant310 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant310 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield311("G(add_belief(perc_rad311(low)))", ["add_belief(perc_rad311(low))", "add_belief(perc_rad311(high))"])
+!inspect_nuclear_plant311 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant311 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield312("G(add_belief(perc_rad312(low)))", ["add_belief(perc_rad312(low))", "add_belief(perc_rad312(high))"])
+!inspect_nuclear_plant312 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant312 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield313("G(add_belief(perc_rad313(low)))", ["add_belief(perc_rad313(low))", "add_belief(perc_rad313(high))"])
+!inspect_nuclear_plant313 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant313 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield314("G(add_belief(perc_rad314(low)))", ["add_belief(perc_rad314(low))", "add_belief(perc_rad314(high))"])
+!inspect_nuclear_plant314 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant314 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield315("G(add_belief(perc_rad315(low)))", ["add_belief(perc_rad315(low))", "add_belief(perc_rad315(high))"])
+!inspect_nuclear_plant315 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant315 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield316("G(add_belief(perc_rad316(low)))", ["add_belief(perc_rad316(low))", "add_belief(perc_rad316(high))"])
+!inspect_nuclear_plant316 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant316 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield317("G(add_belief(perc_rad317(low)))", ["add_belief(perc_rad317(low))", "add_belief(perc_rad317(high))"])
+!inspect_nuclear_plant317 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant317 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield318("G(add_belief(perc_rad318(low)))", ["add_belief(perc_rad318(low))", "add_belief(perc_rad318(high))"])
+!inspect_nuclear_plant318 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant318 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield319("G(add_belief(perc_rad319(low)))", ["add_belief(perc_rad319(low))", "add_belief(perc_rad319(high))"])
+!inspect_nuclear_plant319 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant319 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield320("G(add_belief(perc_rad320(low)))", ["add_belief(perc_rad320(low))", "add_belief(perc_rad320(high))"])
+!inspect_nuclear_plant320 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant320 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield321("G(add_belief(perc_rad321(low)))", ["add_belief(perc_rad321(low))", "add_belief(perc_rad321(high))"])
+!inspect_nuclear_plant321 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant321 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield322("G(add_belief(perc_rad322(low)))", ["add_belief(perc_rad322(low))", "add_belief(perc_rad322(high))"])
+!inspect_nuclear_plant322 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant322 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield323("G(add_belief(perc_rad323(low)))", ["add_belief(perc_rad323(low))", "add_belief(perc_rad323(high))"])
+!inspect_nuclear_plant323 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant323 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield324("G(add_belief(perc_rad324(low)))", ["add_belief(perc_rad324(low))", "add_belief(perc_rad324(high))"])
+!inspect_nuclear_plant324 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant324 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield325("G(add_belief(perc_rad325(low)))", ["add_belief(perc_rad325(low))", "add_belief(perc_rad325(high))"])
+!inspect_nuclear_plant325 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant325 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield326("G(add_belief(perc_rad326(low)))", ["add_belief(perc_rad326(low))", "add_belief(perc_rad326(high))"])
+!inspect_nuclear_plant326 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant326 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield327("G(add_belief(perc_rad327(low)))", ["add_belief(perc_rad327(low))", "add_belief(perc_rad327(high))"])
+!inspect_nuclear_plant327 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant327 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield328("G(add_belief(perc_rad328(low)))", ["add_belief(perc_rad328(low))", "add_belief(perc_rad328(high))"])
+!inspect_nuclear_plant328 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant328 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield329("G(add_belief(perc_rad329(low)))", ["add_belief(perc_rad329(low))", "add_belief(perc_rad329(high))"])
+!inspect_nuclear_plant329 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant329 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield330("G(add_belief(perc_rad330(low)))", ["add_belief(perc_rad330(low))", "add_belief(perc_rad330(high))"])
+!inspect_nuclear_plant330 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant330 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield331("G(add_belief(perc_rad331(low)))", ["add_belief(perc_rad331(low))", "add_belief(perc_rad331(high))"])
+!inspect_nuclear_plant331 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant331 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield332("G(add_belief(perc_rad332(low)))", ["add_belief(perc_rad332(low))", "add_belief(perc_rad332(high))"])
+!inspect_nuclear_plant332 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant332 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield333("G(add_belief(perc_rad333(low)))", ["add_belief(perc_rad333(low))", "add_belief(perc_rad333(high))"])
+!inspect_nuclear_plant333 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant333 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield334("G(add_belief(perc_rad334(low)))", ["add_belief(perc_rad334(low))", "add_belief(perc_rad334(high))"])
+!inspect_nuclear_plant334 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant334 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield335("G(add_belief(perc_rad335(low)))", ["add_belief(perc_rad335(low))", "add_belief(perc_rad335(high))"])
+!inspect_nuclear_plant335 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant335 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield336("G(add_belief(perc_rad336(low)))", ["add_belief(perc_rad336(low))", "add_belief(perc_rad336(high))"])
+!inspect_nuclear_plant336 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant336 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield337("G(add_belief(perc_rad337(low)))", ["add_belief(perc_rad337(low))", "add_belief(perc_rad337(high))"])
+!inspect_nuclear_plant337 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant337 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield338("G(add_belief(perc_rad338(low)))", ["add_belief(perc_rad338(low))", "add_belief(perc_rad338(high))"])
+!inspect_nuclear_plant338 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant338 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield339("G(add_belief(perc_rad339(low)))", ["add_belief(perc_rad339(low))", "add_belief(perc_rad339(high))"])
+!inspect_nuclear_plant339 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant339 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield340("G(add_belief(perc_rad340(low)))", ["add_belief(perc_rad340(low))", "add_belief(perc_rad340(high))"])
+!inspect_nuclear_plant340 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant340 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield341("G(add_belief(perc_rad341(low)))", ["add_belief(perc_rad341(low))", "add_belief(perc_rad341(high))"])
+!inspect_nuclear_plant341 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant341 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield342("G(add_belief(perc_rad342(low)))", ["add_belief(perc_rad342(low))", "add_belief(perc_rad342(high))"])
+!inspect_nuclear_plant342 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant342 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield343("G(add_belief(perc_rad343(low)))", ["add_belief(perc_rad343(low))", "add_belief(perc_rad343(high))"])
+!inspect_nuclear_plant343 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant343 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield344("G(add_belief(perc_rad344(low)))", ["add_belief(perc_rad344(low))", "add_belief(perc_rad344(high))"])
+!inspect_nuclear_plant344 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant344 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield345("G(add_belief(perc_rad345(low)))", ["add_belief(perc_rad345(low))", "add_belief(perc_rad345(high))"])
+!inspect_nuclear_plant345 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant345 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield346("G(add_belief(perc_rad346(low)))", ["add_belief(perc_rad346(low))", "add_belief(perc_rad346(high))"])
+!inspect_nuclear_plant346 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant346 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield347("G(add_belief(perc_rad347(low)))", ["add_belief(perc_rad347(low))", "add_belief(perc_rad347(high))"])
+!inspect_nuclear_plant347 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant347 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield348("G(add_belief(perc_rad348(low)))", ["add_belief(perc_rad348(low))", "add_belief(perc_rad348(high))"])
+!inspect_nuclear_plant348 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant348 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield349("G(add_belief(perc_rad349(low)))", ["add_belief(perc_rad349(low))", "add_belief(perc_rad349(high))"])
+!inspect_nuclear_plant349 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant349 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield350("G(add_belief(perc_rad350(low)))", ["add_belief(perc_rad350(low))", "add_belief(perc_rad350(high))"])
+!inspect_nuclear_plant350 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant350 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield351("G(add_belief(perc_rad351(low)))", ["add_belief(perc_rad351(low))", "add_belief(perc_rad351(high))"])
+!inspect_nuclear_plant351 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant351 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield352("G(add_belief(perc_rad352(low)))", ["add_belief(perc_rad352(low))", "add_belief(perc_rad352(high))"])
+!inspect_nuclear_plant352 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant352 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield353("G(add_belief(perc_rad353(low)))", ["add_belief(perc_rad353(low))", "add_belief(perc_rad353(high))"])
+!inspect_nuclear_plant353 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant353 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield354("G(add_belief(perc_rad354(low)))", ["add_belief(perc_rad354(low))", "add_belief(perc_rad354(high))"])
+!inspect_nuclear_plant354 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant354 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield355("G(add_belief(perc_rad355(low)))", ["add_belief(perc_rad355(low))", "add_belief(perc_rad355(high))"])
+!inspect_nuclear_plant355 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant355 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield356("G(add_belief(perc_rad356(low)))", ["add_belief(perc_rad356(low))", "add_belief(perc_rad356(high))"])
+!inspect_nuclear_plant356 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant356 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield357("G(add_belief(perc_rad357(low)))", ["add_belief(perc_rad357(low))", "add_belief(perc_rad357(high))"])
+!inspect_nuclear_plant357 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant357 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield358("G(add_belief(perc_rad358(low)))", ["add_belief(perc_rad358(low))", "add_belief(perc_rad358(high))"])
+!inspect_nuclear_plant358 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant358 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield359("G(add_belief(perc_rad359(low)))", ["add_belief(perc_rad359(low))", "add_belief(perc_rad359(high))"])
+!inspect_nuclear_plant359 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant359 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield360("G(add_belief(perc_rad360(low)))", ["add_belief(perc_rad360(low))", "add_belief(perc_rad360(high))"])
+!inspect_nuclear_plant360 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant360 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield361("G(add_belief(perc_rad361(low)))", ["add_belief(perc_rad361(low))", "add_belief(perc_rad361(high))"])
+!inspect_nuclear_plant361 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant361 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield362("G(add_belief(perc_rad362(low)))", ["add_belief(perc_rad362(low))", "add_belief(perc_rad362(high))"])
+!inspect_nuclear_plant362 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant362 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield363("G(add_belief(perc_rad363(low)))", ["add_belief(perc_rad363(low))", "add_belief(perc_rad363(high))"])
+!inspect_nuclear_plant363 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant363 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield364("G(add_belief(perc_rad364(low)))", ["add_belief(perc_rad364(low))", "add_belief(perc_rad364(high))"])
+!inspect_nuclear_plant364 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant364 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield365("G(add_belief(perc_rad365(low)))", ["add_belief(perc_rad365(low))", "add_belief(perc_rad365(high))"])
+!inspect_nuclear_plant365 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant365 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield366("G(add_belief(perc_rad366(low)))", ["add_belief(perc_rad366(low))", "add_belief(perc_rad366(high))"])
+!inspect_nuclear_plant366 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant366 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield367("G(add_belief(perc_rad367(low)))", ["add_belief(perc_rad367(low))", "add_belief(perc_rad367(high))"])
+!inspect_nuclear_plant367 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant367 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield368("G(add_belief(perc_rad368(low)))", ["add_belief(perc_rad368(low))", "add_belief(perc_rad368(high))"])
+!inspect_nuclear_plant368 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant368 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield369("G(add_belief(perc_rad369(low)))", ["add_belief(perc_rad369(low))", "add_belief(perc_rad369(high))"])
+!inspect_nuclear_plant369 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant369 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield370("G(add_belief(perc_rad370(low)))", ["add_belief(perc_rad370(low))", "add_belief(perc_rad370(high))"])
+!inspect_nuclear_plant370 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant370 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield371("G(add_belief(perc_rad371(low)))", ["add_belief(perc_rad371(low))", "add_belief(perc_rad371(high))"])
+!inspect_nuclear_plant371 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant371 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield372("G(add_belief(perc_rad372(low)))", ["add_belief(perc_rad372(low))", "add_belief(perc_rad372(high))"])
+!inspect_nuclear_plant372 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant372 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield373("G(add_belief(perc_rad373(low)))", ["add_belief(perc_rad373(low))", "add_belief(perc_rad373(high))"])
+!inspect_nuclear_plant373 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant373 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield374("G(add_belief(perc_rad374(low)))", ["add_belief(perc_rad374(low))", "add_belief(perc_rad374(high))"])
+!inspect_nuclear_plant374 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant374 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield375("G(add_belief(perc_rad375(low)))", ["add_belief(perc_rad375(low))", "add_belief(perc_rad375(high))"])
+!inspect_nuclear_plant375 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant375 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield376("G(add_belief(perc_rad376(low)))", ["add_belief(perc_rad376(low))", "add_belief(perc_rad376(high))"])
+!inspect_nuclear_plant376 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant376 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield377("G(add_belief(perc_rad377(low)))", ["add_belief(perc_rad377(low))", "add_belief(perc_rad377(high))"])
+!inspect_nuclear_plant377 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant377 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield378("G(add_belief(perc_rad378(low)))", ["add_belief(perc_rad378(low))", "add_belief(perc_rad378(high))"])
+!inspect_nuclear_plant378 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant378 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield379("G(add_belief(perc_rad379(low)))", ["add_belief(perc_rad379(low))", "add_belief(perc_rad379(high))"])
+!inspect_nuclear_plant379 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant379 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield380("G(add_belief(perc_rad380(low)))", ["add_belief(perc_rad380(low))", "add_belief(perc_rad380(high))"])
+!inspect_nuclear_plant380 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant380 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield381("G(add_belief(perc_rad381(low)))", ["add_belief(perc_rad381(low))", "add_belief(perc_rad381(high))"])
+!inspect_nuclear_plant381 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant381 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield382("G(add_belief(perc_rad382(low)))", ["add_belief(perc_rad382(low))", "add_belief(perc_rad382(high))"])
+!inspect_nuclear_plant382 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant382 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield383("G(add_belief(perc_rad383(low)))", ["add_belief(perc_rad383(low))", "add_belief(perc_rad383(high))"])
+!inspect_nuclear_plant383 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant383 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield384("G(add_belief(perc_rad384(low)))", ["add_belief(perc_rad384(low))", "add_belief(perc_rad384(high))"])
+!inspect_nuclear_plant384 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant384 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield385("G(add_belief(perc_rad385(low)))", ["add_belief(perc_rad385(low))", "add_belief(perc_rad385(high))"])
+!inspect_nuclear_plant385 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant385 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield386("G(add_belief(perc_rad386(low)))", ["add_belief(perc_rad386(low))", "add_belief(perc_rad386(high))"])
+!inspect_nuclear_plant386 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant386 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield387("G(add_belief(perc_rad387(low)))", ["add_belief(perc_rad387(low))", "add_belief(perc_rad387(high))"])
+!inspect_nuclear_plant387 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant387 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield388("G(add_belief(perc_rad388(low)))", ["add_belief(perc_rad388(low))", "add_belief(perc_rad388(high))"])
+!inspect_nuclear_plant388 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant388 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield389("G(add_belief(perc_rad389(low)))", ["add_belief(perc_rad389(low))", "add_belief(perc_rad389(high))"])
+!inspect_nuclear_plant389 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant389 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield390("G(add_belief(perc_rad390(low)))", ["add_belief(perc_rad390(low))", "add_belief(perc_rad390(high))"])
+!inspect_nuclear_plant390 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant390 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield391("G(add_belief(perc_rad391(low)))", ["add_belief(perc_rad391(low))", "add_belief(perc_rad391(high))"])
+!inspect_nuclear_plant391 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant391 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield392("G(add_belief(perc_rad392(low)))", ["add_belief(perc_rad392(low))", "add_belief(perc_rad392(high))"])
+!inspect_nuclear_plant392 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant392 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield393("G(add_belief(perc_rad393(low)))", ["add_belief(perc_rad393(low))", "add_belief(perc_rad393(high))"])
+!inspect_nuclear_plant393 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant393 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield394("G(add_belief(perc_rad394(low)))", ["add_belief(perc_rad394(low))", "add_belief(perc_rad394(high))"])
+!inspect_nuclear_plant394 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant394 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield395("G(add_belief(perc_rad395(low)))", ["add_belief(perc_rad395(low))", "add_belief(perc_rad395(high))"])
+!inspect_nuclear_plant395 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant395 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield396("G(add_belief(perc_rad396(low)))", ["add_belief(perc_rad396(low))", "add_belief(perc_rad396(high))"])
+!inspect_nuclear_plant396 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant396 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield397("G(add_belief(perc_rad397(low)))", ["add_belief(perc_rad397(low))", "add_belief(perc_rad397(high))"])
+!inspect_nuclear_plant397 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant397 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield398("G(add_belief(perc_rad398(low)))", ["add_belief(perc_rad398(low))", "add_belief(perc_rad398(high))"])
+!inspect_nuclear_plant398 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant398 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield399("G(add_belief(perc_rad399(low)))", ["add_belief(perc_rad399(low))", "add_belief(perc_rad399(high))"])
+!inspect_nuclear_plant399 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant399 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield400("G(add_belief(perc_rad400(low)))", ["add_belief(perc_rad400(low))", "add_belief(perc_rad400(high))"])
+!inspect_nuclear_plant400 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant400 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield401("G(add_belief(perc_rad401(low)))", ["add_belief(perc_rad401(low))", "add_belief(perc_rad401(high))"])
+!inspect_nuclear_plant401 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant401 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield402("G(add_belief(perc_rad402(low)))", ["add_belief(perc_rad402(low))", "add_belief(perc_rad402(high))"])
+!inspect_nuclear_plant402 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant402 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield403("G(add_belief(perc_rad403(low)))", ["add_belief(perc_rad403(low))", "add_belief(perc_rad403(high))"])
+!inspect_nuclear_plant403 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant403 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield404("G(add_belief(perc_rad404(low)))", ["add_belief(perc_rad404(low))", "add_belief(perc_rad404(high))"])
+!inspect_nuclear_plant404 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant404 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield405("G(add_belief(perc_rad405(low)))", ["add_belief(perc_rad405(low))", "add_belief(perc_rad405(high))"])
+!inspect_nuclear_plant405 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant405 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield406("G(add_belief(perc_rad406(low)))", ["add_belief(perc_rad406(low))", "add_belief(perc_rad406(high))"])
+!inspect_nuclear_plant406 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant406 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield407("G(add_belief(perc_rad407(low)))", ["add_belief(perc_rad407(low))", "add_belief(perc_rad407(high))"])
+!inspect_nuclear_plant407 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant407 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield408("G(add_belief(perc_rad408(low)))", ["add_belief(perc_rad408(low))", "add_belief(perc_rad408(high))"])
+!inspect_nuclear_plant408 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant408 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield409("G(add_belief(perc_rad409(low)))", ["add_belief(perc_rad409(low))", "add_belief(perc_rad409(high))"])
+!inspect_nuclear_plant409 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant409 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield410("G(add_belief(perc_rad410(low)))", ["add_belief(perc_rad410(low))", "add_belief(perc_rad410(high))"])
+!inspect_nuclear_plant410 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant410 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield411("G(add_belief(perc_rad411(low)))", ["add_belief(perc_rad411(low))", "add_belief(perc_rad411(high))"])
+!inspect_nuclear_plant411 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant411 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield412("G(add_belief(perc_rad412(low)))", ["add_belief(perc_rad412(low))", "add_belief(perc_rad412(high))"])
+!inspect_nuclear_plant412 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant412 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield413("G(add_belief(perc_rad413(low)))", ["add_belief(perc_rad413(low))", "add_belief(perc_rad413(high))"])
+!inspect_nuclear_plant413 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant413 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield414("G(add_belief(perc_rad414(low)))", ["add_belief(perc_rad414(low))", "add_belief(perc_rad414(high))"])
+!inspect_nuclear_plant414 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant414 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield415("G(add_belief(perc_rad415(low)))", ["add_belief(perc_rad415(low))", "add_belief(perc_rad415(high))"])
+!inspect_nuclear_plant415 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant415 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield416("G(add_belief(perc_rad416(low)))", ["add_belief(perc_rad416(low))", "add_belief(perc_rad416(high))"])
+!inspect_nuclear_plant416 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant416 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield417("G(add_belief(perc_rad417(low)))", ["add_belief(perc_rad417(low))", "add_belief(perc_rad417(high))"])
+!inspect_nuclear_plant417 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant417 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield418("G(add_belief(perc_rad418(low)))", ["add_belief(perc_rad418(low))", "add_belief(perc_rad418(high))"])
+!inspect_nuclear_plant418 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant418 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield419("G(add_belief(perc_rad419(low)))", ["add_belief(perc_rad419(low))", "add_belief(perc_rad419(high))"])
+!inspect_nuclear_plant419 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant419 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield420("G(add_belief(perc_rad420(low)))", ["add_belief(perc_rad420(low))", "add_belief(perc_rad420(high))"])
+!inspect_nuclear_plant420 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant420 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield421("G(add_belief(perc_rad421(low)))", ["add_belief(perc_rad421(low))", "add_belief(perc_rad421(high))"])
+!inspect_nuclear_plant421 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant421 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield422("G(add_belief(perc_rad422(low)))", ["add_belief(perc_rad422(low))", "add_belief(perc_rad422(high))"])
+!inspect_nuclear_plant422 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant422 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield423("G(add_belief(perc_rad423(low)))", ["add_belief(perc_rad423(low))", "add_belief(perc_rad423(high))"])
+!inspect_nuclear_plant423 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant423 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield424("G(add_belief(perc_rad424(low)))", ["add_belief(perc_rad424(low))", "add_belief(perc_rad424(high))"])
+!inspect_nuclear_plant424 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant424 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield425("G(add_belief(perc_rad425(low)))", ["add_belief(perc_rad425(low))", "add_belief(perc_rad425(high))"])
+!inspect_nuclear_plant425 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant425 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield426("G(add_belief(perc_rad426(low)))", ["add_belief(perc_rad426(low))", "add_belief(perc_rad426(high))"])
+!inspect_nuclear_plant426 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant426 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield427("G(add_belief(perc_rad427(low)))", ["add_belief(perc_rad427(low))", "add_belief(perc_rad427(high))"])
+!inspect_nuclear_plant427 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant427 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield428("G(add_belief(perc_rad428(low)))", ["add_belief(perc_rad428(low))", "add_belief(perc_rad428(high))"])
+!inspect_nuclear_plant428 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant428 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield429("G(add_belief(perc_rad429(low)))", ["add_belief(perc_rad429(low))", "add_belief(perc_rad429(high))"])
+!inspect_nuclear_plant429 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant429 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield430("G(add_belief(perc_rad430(low)))", ["add_belief(perc_rad430(low))", "add_belief(perc_rad430(high))"])
+!inspect_nuclear_plant430 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant430 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield431("G(add_belief(perc_rad431(low)))", ["add_belief(perc_rad431(low))", "add_belief(perc_rad431(high))"])
+!inspect_nuclear_plant431 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant431 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield432("G(add_belief(perc_rad432(low)))", ["add_belief(perc_rad432(low))", "add_belief(perc_rad432(high))"])
+!inspect_nuclear_plant432 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant432 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield433("G(add_belief(perc_rad433(low)))", ["add_belief(perc_rad433(low))", "add_belief(perc_rad433(high))"])
+!inspect_nuclear_plant433 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant433 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield434("G(add_belief(perc_rad434(low)))", ["add_belief(perc_rad434(low))", "add_belief(perc_rad434(high))"])
+!inspect_nuclear_plant434 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant434 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield435("G(add_belief(perc_rad435(low)))", ["add_belief(perc_rad435(low))", "add_belief(perc_rad435(high))"])
+!inspect_nuclear_plant435 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant435 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield436("G(add_belief(perc_rad436(low)))", ["add_belief(perc_rad436(low))", "add_belief(perc_rad436(high))"])
+!inspect_nuclear_plant436 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant436 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield437("G(add_belief(perc_rad437(low)))", ["add_belief(perc_rad437(low))", "add_belief(perc_rad437(high))"])
+!inspect_nuclear_plant437 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant437 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield438("G(add_belief(perc_rad438(low)))", ["add_belief(perc_rad438(low))", "add_belief(perc_rad438(high))"])
+!inspect_nuclear_plant438 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant438 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield439("G(add_belief(perc_rad439(low)))", ["add_belief(perc_rad439(low))", "add_belief(perc_rad439(high))"])
+!inspect_nuclear_plant439 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant439 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield440("G(add_belief(perc_rad440(low)))", ["add_belief(perc_rad440(low))", "add_belief(perc_rad440(high))"])
+!inspect_nuclear_plant440 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant440 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield441("G(add_belief(perc_rad441(low)))", ["add_belief(perc_rad441(low))", "add_belief(perc_rad441(high))"])
+!inspect_nuclear_plant441 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant441 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield442("G(add_belief(perc_rad442(low)))", ["add_belief(perc_rad442(low))", "add_belief(perc_rad442(high))"])
+!inspect_nuclear_plant442 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant442 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield443("G(add_belief(perc_rad443(low)))", ["add_belief(perc_rad443(low))", "add_belief(perc_rad443(high))"])
+!inspect_nuclear_plant443 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant443 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield444("G(add_belief(perc_rad444(low)))", ["add_belief(perc_rad444(low))", "add_belief(perc_rad444(high))"])
+!inspect_nuclear_plant444 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant444 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield445("G(add_belief(perc_rad445(low)))", ["add_belief(perc_rad445(low))", "add_belief(perc_rad445(high))"])
+!inspect_nuclear_plant445 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant445 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield446("G(add_belief(perc_rad446(low)))", ["add_belief(perc_rad446(low))", "add_belief(perc_rad446(high))"])
+!inspect_nuclear_plant446 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant446 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield447("G(add_belief(perc_rad447(low)))", ["add_belief(perc_rad447(low))", "add_belief(perc_rad447(high))"])
+!inspect_nuclear_plant447 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant447 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield448("G(add_belief(perc_rad448(low)))", ["add_belief(perc_rad448(low))", "add_belief(perc_rad448(high))"])
+!inspect_nuclear_plant448 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant448 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield449("G(add_belief(perc_rad449(low)))", ["add_belief(perc_rad449(low))", "add_belief(perc_rad449(high))"])
+!inspect_nuclear_plant449 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant449 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield450("G(add_belief(perc_rad450(low)))", ["add_belief(perc_rad450(low))", "add_belief(perc_rad450(high))"])
+!inspect_nuclear_plant450 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant450 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield451("G(add_belief(perc_rad451(low)))", ["add_belief(perc_rad451(low))", "add_belief(perc_rad451(high))"])
+!inspect_nuclear_plant451 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant451 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield452("G(add_belief(perc_rad452(low)))", ["add_belief(perc_rad452(low))", "add_belief(perc_rad452(high))"])
+!inspect_nuclear_plant452 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant452 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield453("G(add_belief(perc_rad453(low)))", ["add_belief(perc_rad453(low))", "add_belief(perc_rad453(high))"])
+!inspect_nuclear_plant453 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant453 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield454("G(add_belief(perc_rad454(low)))", ["add_belief(perc_rad454(low))", "add_belief(perc_rad454(high))"])
+!inspect_nuclear_plant454 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant454 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield455("G(add_belief(perc_rad455(low)))", ["add_belief(perc_rad455(low))", "add_belief(perc_rad455(high))"])
+!inspect_nuclear_plant455 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant455 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield456("G(add_belief(perc_rad456(low)))", ["add_belief(perc_rad456(low))", "add_belief(perc_rad456(high))"])
+!inspect_nuclear_plant456 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant456 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield457("G(add_belief(perc_rad457(low)))", ["add_belief(perc_rad457(low))", "add_belief(perc_rad457(high))"])
+!inspect_nuclear_plant457 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant457 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield458("G(add_belief(perc_rad458(low)))", ["add_belief(perc_rad458(low))", "add_belief(perc_rad458(high))"])
+!inspect_nuclear_plant458 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant458 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield459("G(add_belief(perc_rad459(low)))", ["add_belief(perc_rad459(low))", "add_belief(perc_rad459(high))"])
+!inspect_nuclear_plant459 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant459 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield460("G(add_belief(perc_rad460(low)))", ["add_belief(perc_rad460(low))", "add_belief(perc_rad460(high))"])
+!inspect_nuclear_plant460 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant460 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield461("G(add_belief(perc_rad461(low)))", ["add_belief(perc_rad461(low))", "add_belief(perc_rad461(high))"])
+!inspect_nuclear_plant461 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant461 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield462("G(add_belief(perc_rad462(low)))", ["add_belief(perc_rad462(low))", "add_belief(perc_rad462(high))"])
+!inspect_nuclear_plant462 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant462 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield463("G(add_belief(perc_rad463(low)))", ["add_belief(perc_rad463(low))", "add_belief(perc_rad463(high))"])
+!inspect_nuclear_plant463 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant463 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield464("G(add_belief(perc_rad464(low)))", ["add_belief(perc_rad464(low))", "add_belief(perc_rad464(high))"])
+!inspect_nuclear_plant464 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant464 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield465("G(add_belief(perc_rad465(low)))", ["add_belief(perc_rad465(low))", "add_belief(perc_rad465(high))"])
+!inspect_nuclear_plant465 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant465 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield466("G(add_belief(perc_rad466(low)))", ["add_belief(perc_rad466(low))", "add_belief(perc_rad466(high))"])
+!inspect_nuclear_plant466 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant466 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield467("G(add_belief(perc_rad467(low)))", ["add_belief(perc_rad467(low))", "add_belief(perc_rad467(high))"])
+!inspect_nuclear_plant467 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant467 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield468("G(add_belief(perc_rad468(low)))", ["add_belief(perc_rad468(low))", "add_belief(perc_rad468(high))"])
+!inspect_nuclear_plant468 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant468 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield469("G(add_belief(perc_rad469(low)))", ["add_belief(perc_rad469(low))", "add_belief(perc_rad469(high))"])
+!inspect_nuclear_plant469 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant469 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield470("G(add_belief(perc_rad470(low)))", ["add_belief(perc_rad470(low))", "add_belief(perc_rad470(high))"])
+!inspect_nuclear_plant470 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant470 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield471("G(add_belief(perc_rad471(low)))", ["add_belief(perc_rad471(low))", "add_belief(perc_rad471(high))"])
+!inspect_nuclear_plant471 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant471 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield472("G(add_belief(perc_rad472(low)))", ["add_belief(perc_rad472(low))", "add_belief(perc_rad472(high))"])
+!inspect_nuclear_plant472 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant472 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield473("G(add_belief(perc_rad473(low)))", ["add_belief(perc_rad473(low))", "add_belief(perc_rad473(high))"])
+!inspect_nuclear_plant473 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant473 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield474("G(add_belief(perc_rad474(low)))", ["add_belief(perc_rad474(low))", "add_belief(perc_rad474(high))"])
+!inspect_nuclear_plant474 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant474 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield475("G(add_belief(perc_rad475(low)))", ["add_belief(perc_rad475(low))", "add_belief(perc_rad475(high))"])
+!inspect_nuclear_plant475 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant475 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield476("G(add_belief(perc_rad476(low)))", ["add_belief(perc_rad476(low))", "add_belief(perc_rad476(high))"])
+!inspect_nuclear_plant476 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant476 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield477("G(add_belief(perc_rad477(low)))", ["add_belief(perc_rad477(low))", "add_belief(perc_rad477(high))"])
+!inspect_nuclear_plant477 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant477 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield478("G(add_belief(perc_rad478(low)))", ["add_belief(perc_rad478(low))", "add_belief(perc_rad478(high))"])
+!inspect_nuclear_plant478 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant478 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield479("G(add_belief(perc_rad479(low)))", ["add_belief(perc_rad479(low))", "add_belief(perc_rad479(high))"])
+!inspect_nuclear_plant479 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant479 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield480("G(add_belief(perc_rad480(low)))", ["add_belief(perc_rad480(low))", "add_belief(perc_rad480(high))"])
+!inspect_nuclear_plant480 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant480 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield481("G(add_belief(perc_rad481(low)))", ["add_belief(perc_rad481(low))", "add_belief(perc_rad481(high))"])
+!inspect_nuclear_plant481 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant481 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield482("G(add_belief(perc_rad482(low)))", ["add_belief(perc_rad482(low))", "add_belief(perc_rad482(high))"])
+!inspect_nuclear_plant482 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant482 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield483("G(add_belief(perc_rad483(low)))", ["add_belief(perc_rad483(low))", "add_belief(perc_rad483(high))"])
+!inspect_nuclear_plant483 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant483 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield484("G(add_belief(perc_rad484(low)))", ["add_belief(perc_rad484(low))", "add_belief(perc_rad484(high))"])
+!inspect_nuclear_plant484 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant484 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield485("G(add_belief(perc_rad485(low)))", ["add_belief(perc_rad485(low))", "add_belief(perc_rad485(high))"])
+!inspect_nuclear_plant485 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant485 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield486("G(add_belief(perc_rad486(low)))", ["add_belief(perc_rad486(low))", "add_belief(perc_rad486(high))"])
+!inspect_nuclear_plant486 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant486 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield487("G(add_belief(perc_rad487(low)))", ["add_belief(perc_rad487(low))", "add_belief(perc_rad487(high))"])
+!inspect_nuclear_plant487 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant487 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield488("G(add_belief(perc_rad488(low)))", ["add_belief(perc_rad488(low))", "add_belief(perc_rad488(high))"])
+!inspect_nuclear_plant488 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant488 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield489("G(add_belief(perc_rad489(low)))", ["add_belief(perc_rad489(low))", "add_belief(perc_rad489(high))"])
+!inspect_nuclear_plant489 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant489 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield490("G(add_belief(perc_rad490(low)))", ["add_belief(perc_rad490(low))", "add_belief(perc_rad490(high))"])
+!inspect_nuclear_plant490 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant490 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield491("G(add_belief(perc_rad491(low)))", ["add_belief(perc_rad491(low))", "add_belief(perc_rad491(high))"])
+!inspect_nuclear_plant491 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant491 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield492("G(add_belief(perc_rad492(low)))", ["add_belief(perc_rad492(low))", "add_belief(perc_rad492(high))"])
+!inspect_nuclear_plant492 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant492 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield493("G(add_belief(perc_rad493(low)))", ["add_belief(perc_rad493(low))", "add_belief(perc_rad493(high))"])
+!inspect_nuclear_plant493 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant493 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield494("G(add_belief(perc_rad494(low)))", ["add_belief(perc_rad494(low))", "add_belief(perc_rad494(high))"])
+!inspect_nuclear_plant494 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant494 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield495("G(add_belief(perc_rad495(low)))", ["add_belief(perc_rad495(low))", "add_belief(perc_rad495(high))"])
+!inspect_nuclear_plant495 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant495 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield496("G(add_belief(perc_rad496(low)))", ["add_belief(perc_rad496(low))", "add_belief(perc_rad496(high))"])
+!inspect_nuclear_plant496 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant496 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield497("G(add_belief(perc_rad497(low)))", ["add_belief(perc_rad497(low))", "add_belief(perc_rad497(high))"])
+!inspect_nuclear_plant497 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant497 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield498("G(add_belief(perc_rad498(low)))", ["add_belief(perc_rad498(low))", "add_belief(perc_rad498(high))"])
+!inspect_nuclear_plant498 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant498 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield499("G(add_belief(perc_rad499(low)))", ["add_belief(perc_rad499(low))", "add_belief(perc_rad499(high))"])
+!inspect_nuclear_plant499 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant499 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield500("G(add_belief(perc_rad500(low)))", ["add_belief(perc_rad500(low))", "add_belief(perc_rad500(high))"])
+!inspect_nuclear_plant500 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant500 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield501("G(add_belief(perc_rad501(low)))", ["add_belief(perc_rad501(low))", "add_belief(perc_rad501(high))"])
+!inspect_nuclear_plant501 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant501 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield502("G(add_belief(perc_rad502(low)))", ["add_belief(perc_rad502(low))", "add_belief(perc_rad502(high))"])
+!inspect_nuclear_plant502 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant502 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield503("G(add_belief(perc_rad503(low)))", ["add_belief(perc_rad503(low))", "add_belief(perc_rad503(high))"])
+!inspect_nuclear_plant503 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant503 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield504("G(add_belief(perc_rad504(low)))", ["add_belief(perc_rad504(low))", "add_belief(perc_rad504(high))"])
+!inspect_nuclear_plant504 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant504 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield505("G(add_belief(perc_rad505(low)))", ["add_belief(perc_rad505(low))", "add_belief(perc_rad505(high))"])
+!inspect_nuclear_plant505 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant505 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield506("G(add_belief(perc_rad506(low)))", ["add_belief(perc_rad506(low))", "add_belief(perc_rad506(high))"])
+!inspect_nuclear_plant506 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant506 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield507("G(add_belief(perc_rad507(low)))", ["add_belief(perc_rad507(low))", "add_belief(perc_rad507(high))"])
+!inspect_nuclear_plant507 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant507 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield508("G(add_belief(perc_rad508(low)))", ["add_belief(perc_rad508(low))", "add_belief(perc_rad508(high))"])
+!inspect_nuclear_plant508 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant508 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield509("G(add_belief(perc_rad509(low)))", ["add_belief(perc_rad509(low))", "add_belief(perc_rad509(high))"])
+!inspect_nuclear_plant509 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant509 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield510("G(add_belief(perc_rad510(low)))", ["add_belief(perc_rad510(low))", "add_belief(perc_rad510(high))"])
+!inspect_nuclear_plant510 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant510 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield511("G(add_belief(perc_rad511(low)))", ["add_belief(perc_rad511(low))", "add_belief(perc_rad511(high))"])
+!inspect_nuclear_plant511 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant511 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield512("G(add_belief(perc_rad512(low)))", ["add_belief(perc_rad512(low))", "add_belief(perc_rad512(high))"])
+!inspect_nuclear_plant512 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant512 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield513("G(add_belief(perc_rad513(low)))", ["add_belief(perc_rad513(low))", "add_belief(perc_rad513(high))"])
+!inspect_nuclear_plant513 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant513 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield514("G(add_belief(perc_rad514(low)))", ["add_belief(perc_rad514(low))", "add_belief(perc_rad514(high))"])
+!inspect_nuclear_plant514 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant514 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield515("G(add_belief(perc_rad515(low)))", ["add_belief(perc_rad515(low))", "add_belief(perc_rad515(high))"])
+!inspect_nuclear_plant515 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant515 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield516("G(add_belief(perc_rad516(low)))", ["add_belief(perc_rad516(low))", "add_belief(perc_rad516(high))"])
+!inspect_nuclear_plant516 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant516 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield517("G(add_belief(perc_rad517(low)))", ["add_belief(perc_rad517(low))", "add_belief(perc_rad517(high))"])
+!inspect_nuclear_plant517 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant517 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield518("G(add_belief(perc_rad518(low)))", ["add_belief(perc_rad518(low))", "add_belief(perc_rad518(high))"])
+!inspect_nuclear_plant518 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant518 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield519("G(add_belief(perc_rad519(low)))", ["add_belief(perc_rad519(low))", "add_belief(perc_rad519(high))"])
+!inspect_nuclear_plant519 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant519 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield520("G(add_belief(perc_rad520(low)))", ["add_belief(perc_rad520(low))", "add_belief(perc_rad520(high))"])
+!inspect_nuclear_plant520 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant520 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield521("G(add_belief(perc_rad521(low)))", ["add_belief(perc_rad521(low))", "add_belief(perc_rad521(high))"])
+!inspect_nuclear_plant521 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant521 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield522("G(add_belief(perc_rad522(low)))", ["add_belief(perc_rad522(low))", "add_belief(perc_rad522(high))"])
+!inspect_nuclear_plant522 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant522 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield523("G(add_belief(perc_rad523(low)))", ["add_belief(perc_rad523(low))", "add_belief(perc_rad523(high))"])
+!inspect_nuclear_plant523 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant523 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield524("G(add_belief(perc_rad524(low)))", ["add_belief(perc_rad524(low))", "add_belief(perc_rad524(high))"])
+!inspect_nuclear_plant524 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant524 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield525("G(add_belief(perc_rad525(low)))", ["add_belief(perc_rad525(low))", "add_belief(perc_rad525(high))"])
+!inspect_nuclear_plant525 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant525 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield526("G(add_belief(perc_rad526(low)))", ["add_belief(perc_rad526(low))", "add_belief(perc_rad526(high))"])
+!inspect_nuclear_plant526 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant526 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield527("G(add_belief(perc_rad527(low)))", ["add_belief(perc_rad527(low))", "add_belief(perc_rad527(high))"])
+!inspect_nuclear_plant527 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant527 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield528("G(add_belief(perc_rad528(low)))", ["add_belief(perc_rad528(low))", "add_belief(perc_rad528(high))"])
+!inspect_nuclear_plant528 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant528 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield529("G(add_belief(perc_rad529(low)))", ["add_belief(perc_rad529(low))", "add_belief(perc_rad529(high))"])
+!inspect_nuclear_plant529 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant529 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield530("G(add_belief(perc_rad530(low)))", ["add_belief(perc_rad530(low))", "add_belief(perc_rad530(high))"])
+!inspect_nuclear_plant530 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant530 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield531("G(add_belief(perc_rad531(low)))", ["add_belief(perc_rad531(low))", "add_belief(perc_rad531(high))"])
+!inspect_nuclear_plant531 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant531 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield532("G(add_belief(perc_rad532(low)))", ["add_belief(perc_rad532(low))", "add_belief(perc_rad532(high))"])
+!inspect_nuclear_plant532 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant532 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield533("G(add_belief(perc_rad533(low)))", ["add_belief(perc_rad533(low))", "add_belief(perc_rad533(high))"])
+!inspect_nuclear_plant533 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant533 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield534("G(add_belief(perc_rad534(low)))", ["add_belief(perc_rad534(low))", "add_belief(perc_rad534(high))"])
+!inspect_nuclear_plant534 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant534 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield535("G(add_belief(perc_rad535(low)))", ["add_belief(perc_rad535(low))", "add_belief(perc_rad535(high))"])
+!inspect_nuclear_plant535 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant535 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield536("G(add_belief(perc_rad536(low)))", ["add_belief(perc_rad536(low))", "add_belief(perc_rad536(high))"])
+!inspect_nuclear_plant536 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant536 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield537("G(add_belief(perc_rad537(low)))", ["add_belief(perc_rad537(low))", "add_belief(perc_rad537(high))"])
+!inspect_nuclear_plant537 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant537 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield538("G(add_belief(perc_rad538(low)))", ["add_belief(perc_rad538(low))", "add_belief(perc_rad538(high))"])
+!inspect_nuclear_plant538 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant538 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield539("G(add_belief(perc_rad539(low)))", ["add_belief(perc_rad539(low))", "add_belief(perc_rad539(high))"])
+!inspect_nuclear_plant539 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant539 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield540("G(add_belief(perc_rad540(low)))", ["add_belief(perc_rad540(low))", "add_belief(perc_rad540(high))"])
+!inspect_nuclear_plant540 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant540 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield541("G(add_belief(perc_rad541(low)))", ["add_belief(perc_rad541(low))", "add_belief(perc_rad541(high))"])
+!inspect_nuclear_plant541 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant541 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield542("G(add_belief(perc_rad542(low)))", ["add_belief(perc_rad542(low))", "add_belief(perc_rad542(high))"])
+!inspect_nuclear_plant542 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant542 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield543("G(add_belief(perc_rad543(low)))", ["add_belief(perc_rad543(low))", "add_belief(perc_rad543(high))"])
+!inspect_nuclear_plant543 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant543 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield544("G(add_belief(perc_rad544(low)))", ["add_belief(perc_rad544(low))", "add_belief(perc_rad544(high))"])
+!inspect_nuclear_plant544 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant544 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield545("G(add_belief(perc_rad545(low)))", ["add_belief(perc_rad545(low))", "add_belief(perc_rad545(high))"])
+!inspect_nuclear_plant545 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant545 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield546("G(add_belief(perc_rad546(low)))", ["add_belief(perc_rad546(low))", "add_belief(perc_rad546(high))"])
+!inspect_nuclear_plant546 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant546 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield547("G(add_belief(perc_rad547(low)))", ["add_belief(perc_rad547(low))", "add_belief(perc_rad547(high))"])
+!inspect_nuclear_plant547 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant547 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield548("G(add_belief(perc_rad548(low)))", ["add_belief(perc_rad548(low))", "add_belief(perc_rad548(high))"])
+!inspect_nuclear_plant548 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant548 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield549("G(add_belief(perc_rad549(low)))", ["add_belief(perc_rad549(low))", "add_belief(perc_rad549(high))"])
+!inspect_nuclear_plant549 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant549 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield550("G(add_belief(perc_rad550(low)))", ["add_belief(perc_rad550(low))", "add_belief(perc_rad550(high))"])
+!inspect_nuclear_plant550 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant550 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield551("G(add_belief(perc_rad551(low)))", ["add_belief(perc_rad551(low))", "add_belief(perc_rad551(high))"])
+!inspect_nuclear_plant551 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant551 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield552("G(add_belief(perc_rad552(low)))", ["add_belief(perc_rad552(low))", "add_belief(perc_rad552(high))"])
+!inspect_nuclear_plant552 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant552 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield553("G(add_belief(perc_rad553(low)))", ["add_belief(perc_rad553(low))", "add_belief(perc_rad553(high))"])
+!inspect_nuclear_plant553 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant553 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield554("G(add_belief(perc_rad554(low)))", ["add_belief(perc_rad554(low))", "add_belief(perc_rad554(high))"])
+!inspect_nuclear_plant554 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant554 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield555("G(add_belief(perc_rad555(low)))", ["add_belief(perc_rad555(low))", "add_belief(perc_rad555(high))"])
+!inspect_nuclear_plant555 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant555 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield556("G(add_belief(perc_rad556(low)))", ["add_belief(perc_rad556(low))", "add_belief(perc_rad556(high))"])
+!inspect_nuclear_plant556 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant556 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield557("G(add_belief(perc_rad557(low)))", ["add_belief(perc_rad557(low))", "add_belief(perc_rad557(high))"])
+!inspect_nuclear_plant557 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant557 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield558("G(add_belief(perc_rad558(low)))", ["add_belief(perc_rad558(low))", "add_belief(perc_rad558(high))"])
+!inspect_nuclear_plant558 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant558 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield559("G(add_belief(perc_rad559(low)))", ["add_belief(perc_rad559(low))", "add_belief(perc_rad559(high))"])
+!inspect_nuclear_plant559 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant559 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield560("G(add_belief(perc_rad560(low)))", ["add_belief(perc_rad560(low))", "add_belief(perc_rad560(high))"])
+!inspect_nuclear_plant560 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant560 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield561("G(add_belief(perc_rad561(low)))", ["add_belief(perc_rad561(low))", "add_belief(perc_rad561(high))"])
+!inspect_nuclear_plant561 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant561 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield562("G(add_belief(perc_rad562(low)))", ["add_belief(perc_rad562(low))", "add_belief(perc_rad562(high))"])
+!inspect_nuclear_plant562 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant562 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield563("G(add_belief(perc_rad563(low)))", ["add_belief(perc_rad563(low))", "add_belief(perc_rad563(high))"])
+!inspect_nuclear_plant563 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant563 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield564("G(add_belief(perc_rad564(low)))", ["add_belief(perc_rad564(low))", "add_belief(perc_rad564(high))"])
+!inspect_nuclear_plant564 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant564 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield565("G(add_belief(perc_rad565(low)))", ["add_belief(perc_rad565(low))", "add_belief(perc_rad565(high))"])
+!inspect_nuclear_plant565 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant565 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield566("G(add_belief(perc_rad566(low)))", ["add_belief(perc_rad566(low))", "add_belief(perc_rad566(high))"])
+!inspect_nuclear_plant566 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant566 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield567("G(add_belief(perc_rad567(low)))", ["add_belief(perc_rad567(low))", "add_belief(perc_rad567(high))"])
+!inspect_nuclear_plant567 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant567 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield568("G(add_belief(perc_rad568(low)))", ["add_belief(perc_rad568(low))", "add_belief(perc_rad568(high))"])
+!inspect_nuclear_plant568 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant568 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield569("G(add_belief(perc_rad569(low)))", ["add_belief(perc_rad569(low))", "add_belief(perc_rad569(high))"])
+!inspect_nuclear_plant569 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant569 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield570("G(add_belief(perc_rad570(low)))", ["add_belief(perc_rad570(low))", "add_belief(perc_rad570(high))"])
+!inspect_nuclear_plant570 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant570 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield571("G(add_belief(perc_rad571(low)))", ["add_belief(perc_rad571(low))", "add_belief(perc_rad571(high))"])
+!inspect_nuclear_plant571 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant571 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield572("G(add_belief(perc_rad572(low)))", ["add_belief(perc_rad572(low))", "add_belief(perc_rad572(high))"])
+!inspect_nuclear_plant572 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant572 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield573("G(add_belief(perc_rad573(low)))", ["add_belief(perc_rad573(low))", "add_belief(perc_rad573(high))"])
+!inspect_nuclear_plant573 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant573 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield574("G(add_belief(perc_rad574(low)))", ["add_belief(perc_rad574(low))", "add_belief(perc_rad574(high))"])
+!inspect_nuclear_plant574 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant574 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield575("G(add_belief(perc_rad575(low)))", ["add_belief(perc_rad575(low))", "add_belief(perc_rad575(high))"])
+!inspect_nuclear_plant575 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant575 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield576("G(add_belief(perc_rad576(low)))", ["add_belief(perc_rad576(low))", "add_belief(perc_rad576(high))"])
+!inspect_nuclear_plant576 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant576 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield577("G(add_belief(perc_rad577(low)))", ["add_belief(perc_rad577(low))", "add_belief(perc_rad577(high))"])
+!inspect_nuclear_plant577 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant577 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield578("G(add_belief(perc_rad578(low)))", ["add_belief(perc_rad578(low))", "add_belief(perc_rad578(high))"])
+!inspect_nuclear_plant578 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant578 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield579("G(add_belief(perc_rad579(low)))", ["add_belief(perc_rad579(low))", "add_belief(perc_rad579(high))"])
+!inspect_nuclear_plant579 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant579 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield580("G(add_belief(perc_rad580(low)))", ["add_belief(perc_rad580(low))", "add_belief(perc_rad580(high))"])
+!inspect_nuclear_plant580 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant580 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield581("G(add_belief(perc_rad581(low)))", ["add_belief(perc_rad581(low))", "add_belief(perc_rad581(high))"])
+!inspect_nuclear_plant581 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant581 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield582("G(add_belief(perc_rad582(low)))", ["add_belief(perc_rad582(low))", "add_belief(perc_rad582(high))"])
+!inspect_nuclear_plant582 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant582 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield583("G(add_belief(perc_rad583(low)))", ["add_belief(perc_rad583(low))", "add_belief(perc_rad583(high))"])
+!inspect_nuclear_plant583 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant583 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield584("G(add_belief(perc_rad584(low)))", ["add_belief(perc_rad584(low))", "add_belief(perc_rad584(high))"])
+!inspect_nuclear_plant584 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant584 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield585("G(add_belief(perc_rad585(low)))", ["add_belief(perc_rad585(low))", "add_belief(perc_rad585(high))"])
+!inspect_nuclear_plant585 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant585 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield586("G(add_belief(perc_rad586(low)))", ["add_belief(perc_rad586(low))", "add_belief(perc_rad586(high))"])
+!inspect_nuclear_plant586 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant586 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield587("G(add_belief(perc_rad587(low)))", ["add_belief(perc_rad587(low))", "add_belief(perc_rad587(high))"])
+!inspect_nuclear_plant587 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant587 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield588("G(add_belief(perc_rad588(low)))", ["add_belief(perc_rad588(low))", "add_belief(perc_rad588(high))"])
+!inspect_nuclear_plant588 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant588 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield589("G(add_belief(perc_rad589(low)))", ["add_belief(perc_rad589(low))", "add_belief(perc_rad589(high))"])
+!inspect_nuclear_plant589 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant589 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield590("G(add_belief(perc_rad590(low)))", ["add_belief(perc_rad590(low))", "add_belief(perc_rad590(high))"])
+!inspect_nuclear_plant590 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant590 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield591("G(add_belief(perc_rad591(low)))", ["add_belief(perc_rad591(low))", "add_belief(perc_rad591(high))"])
+!inspect_nuclear_plant591 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant591 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield592("G(add_belief(perc_rad592(low)))", ["add_belief(perc_rad592(low))", "add_belief(perc_rad592(high))"])
+!inspect_nuclear_plant592 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant592 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield593("G(add_belief(perc_rad593(low)))", ["add_belief(perc_rad593(low))", "add_belief(perc_rad593(high))"])
+!inspect_nuclear_plant593 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant593 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield594("G(add_belief(perc_rad594(low)))", ["add_belief(perc_rad594(low))", "add_belief(perc_rad594(high))"])
+!inspect_nuclear_plant594 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant594 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield595("G(add_belief(perc_rad595(low)))", ["add_belief(perc_rad595(low))", "add_belief(perc_rad595(high))"])
+!inspect_nuclear_plant595 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant595 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield596("G(add_belief(perc_rad596(low)))", ["add_belief(perc_rad596(low))", "add_belief(perc_rad596(high))"])
+!inspect_nuclear_plant596 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant596 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield597("G(add_belief(perc_rad597(low)))", ["add_belief(perc_rad597(low))", "add_belief(perc_rad597(high))"])
+!inspect_nuclear_plant597 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant597 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield598("G(add_belief(perc_rad598(low)))", ["add_belief(perc_rad598(low))", "add_belief(perc_rad598(high))"])
+!inspect_nuclear_plant598 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant598 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield599("G(add_belief(perc_rad599(low)))", ["add_belief(perc_rad599(low))", "add_belief(perc_rad599(high))"])
+!inspect_nuclear_plant599 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant599 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield600("G(add_belief(perc_rad600(low)))", ["add_belief(perc_rad600(low))", "add_belief(perc_rad600(high))"])
+!inspect_nuclear_plant600 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant600 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield601("G(add_belief(perc_rad601(low)))", ["add_belief(perc_rad601(low))", "add_belief(perc_rad601(high))"])
+!inspect_nuclear_plant601 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant601 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield602("G(add_belief(perc_rad602(low)))", ["add_belief(perc_rad602(low))", "add_belief(perc_rad602(high))"])
+!inspect_nuclear_plant602 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant602 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield603("G(add_belief(perc_rad603(low)))", ["add_belief(perc_rad603(low))", "add_belief(perc_rad603(high))"])
+!inspect_nuclear_plant603 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant603 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield604("G(add_belief(perc_rad604(low)))", ["add_belief(perc_rad604(low))", "add_belief(perc_rad604(high))"])
+!inspect_nuclear_plant604 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant604 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield605("G(add_belief(perc_rad605(low)))", ["add_belief(perc_rad605(low))", "add_belief(perc_rad605(high))"])
+!inspect_nuclear_plant605 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant605 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield606("G(add_belief(perc_rad606(low)))", ["add_belief(perc_rad606(low))", "add_belief(perc_rad606(high))"])
+!inspect_nuclear_plant606 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant606 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield607("G(add_belief(perc_rad607(low)))", ["add_belief(perc_rad607(low))", "add_belief(perc_rad607(high))"])
+!inspect_nuclear_plant607 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant607 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield608("G(add_belief(perc_rad608(low)))", ["add_belief(perc_rad608(low))", "add_belief(perc_rad608(high))"])
+!inspect_nuclear_plant608 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant608 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield609("G(add_belief(perc_rad609(low)))", ["add_belief(perc_rad609(low))", "add_belief(perc_rad609(high))"])
+!inspect_nuclear_plant609 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant609 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield610("G(add_belief(perc_rad610(low)))", ["add_belief(perc_rad610(low))", "add_belief(perc_rad610(high))"])
+!inspect_nuclear_plant610 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant610 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield611("G(add_belief(perc_rad611(low)))", ["add_belief(perc_rad611(low))", "add_belief(perc_rad611(high))"])
+!inspect_nuclear_plant611 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant611 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield612("G(add_belief(perc_rad612(low)))", ["add_belief(perc_rad612(low))", "add_belief(perc_rad612(high))"])
+!inspect_nuclear_plant612 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant612 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield613("G(add_belief(perc_rad613(low)))", ["add_belief(perc_rad613(low))", "add_belief(perc_rad613(high))"])
+!inspect_nuclear_plant613 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant613 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield614("G(add_belief(perc_rad614(low)))", ["add_belief(perc_rad614(low))", "add_belief(perc_rad614(high))"])
+!inspect_nuclear_plant614 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant614 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield615("G(add_belief(perc_rad615(low)))", ["add_belief(perc_rad615(low))", "add_belief(perc_rad615(high))"])
+!inspect_nuclear_plant615 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant615 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield616("G(add_belief(perc_rad616(low)))", ["add_belief(perc_rad616(low))", "add_belief(perc_rad616(high))"])
+!inspect_nuclear_plant616 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant616 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield617("G(add_belief(perc_rad617(low)))", ["add_belief(perc_rad617(low))", "add_belief(perc_rad617(high))"])
+!inspect_nuclear_plant617 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant617 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield618("G(add_belief(perc_rad618(low)))", ["add_belief(perc_rad618(low))", "add_belief(perc_rad618(high))"])
+!inspect_nuclear_plant618 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant618 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield619("G(add_belief(perc_rad619(low)))", ["add_belief(perc_rad619(low))", "add_belief(perc_rad619(high))"])
+!inspect_nuclear_plant619 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant619 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield620("G(add_belief(perc_rad620(low)))", ["add_belief(perc_rad620(low))", "add_belief(perc_rad620(high))"])
+!inspect_nuclear_plant620 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant620 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield621("G(add_belief(perc_rad621(low)))", ["add_belief(perc_rad621(low))", "add_belief(perc_rad621(high))"])
+!inspect_nuclear_plant621 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant621 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield622("G(add_belief(perc_rad622(low)))", ["add_belief(perc_rad622(low))", "add_belief(perc_rad622(high))"])
+!inspect_nuclear_plant622 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant622 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield623("G(add_belief(perc_rad623(low)))", ["add_belief(perc_rad623(low))", "add_belief(perc_rad623(high))"])
+!inspect_nuclear_plant623 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant623 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield624("G(add_belief(perc_rad624(low)))", ["add_belief(perc_rad624(low))", "add_belief(perc_rad624(high))"])
+!inspect_nuclear_plant624 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant624 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield625("G(add_belief(perc_rad625(low)))", ["add_belief(perc_rad625(low))", "add_belief(perc_rad625(high))"])
+!inspect_nuclear_plant625 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant625 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield626("G(add_belief(perc_rad626(low)))", ["add_belief(perc_rad626(low))", "add_belief(perc_rad626(high))"])
+!inspect_nuclear_plant626 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant626 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield627("G(add_belief(perc_rad627(low)))", ["add_belief(perc_rad627(low))", "add_belief(perc_rad627(high))"])
+!inspect_nuclear_plant627 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant627 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield628("G(add_belief(perc_rad628(low)))", ["add_belief(perc_rad628(low))", "add_belief(perc_rad628(high))"])
+!inspect_nuclear_plant628 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant628 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield629("G(add_belief(perc_rad629(low)))", ["add_belief(perc_rad629(low))", "add_belief(perc_rad629(high))"])
+!inspect_nuclear_plant629 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant629 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield630("G(add_belief(perc_rad630(low)))", ["add_belief(perc_rad630(low))", "add_belief(perc_rad630(high))"])
+!inspect_nuclear_plant630 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant630 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield631("G(add_belief(perc_rad631(low)))", ["add_belief(perc_rad631(low))", "add_belief(perc_rad631(high))"])
+!inspect_nuclear_plant631 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant631 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield632("G(add_belief(perc_rad632(low)))", ["add_belief(perc_rad632(low))", "add_belief(perc_rad632(high))"])
+!inspect_nuclear_plant632 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant632 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield633("G(add_belief(perc_rad633(low)))", ["add_belief(perc_rad633(low))", "add_belief(perc_rad633(high))"])
+!inspect_nuclear_plant633 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant633 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield634("G(add_belief(perc_rad634(low)))", ["add_belief(perc_rad634(low))", "add_belief(perc_rad634(high))"])
+!inspect_nuclear_plant634 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant634 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield635("G(add_belief(perc_rad635(low)))", ["add_belief(perc_rad635(low))", "add_belief(perc_rad635(high))"])
+!inspect_nuclear_plant635 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant635 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield636("G(add_belief(perc_rad636(low)))", ["add_belief(perc_rad636(low))", "add_belief(perc_rad636(high))"])
+!inspect_nuclear_plant636 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant636 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield637("G(add_belief(perc_rad637(low)))", ["add_belief(perc_rad637(low))", "add_belief(perc_rad637(high))"])
+!inspect_nuclear_plant637 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant637 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield638("G(add_belief(perc_rad638(low)))", ["add_belief(perc_rad638(low))", "add_belief(perc_rad638(high))"])
+!inspect_nuclear_plant638 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant638 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield639("G(add_belief(perc_rad639(low)))", ["add_belief(perc_rad639(low))", "add_belief(perc_rad639(high))"])
+!inspect_nuclear_plant639 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant639 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield640("G(add_belief(perc_rad640(low)))", ["add_belief(perc_rad640(low))", "add_belief(perc_rad640(high))"])
+!inspect_nuclear_plant640 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant640 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield641("G(add_belief(perc_rad641(low)))", ["add_belief(perc_rad641(low))", "add_belief(perc_rad641(high))"])
+!inspect_nuclear_plant641 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant641 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield642("G(add_belief(perc_rad642(low)))", ["add_belief(perc_rad642(low))", "add_belief(perc_rad642(high))"])
+!inspect_nuclear_plant642 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant642 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield643("G(add_belief(perc_rad643(low)))", ["add_belief(perc_rad643(low))", "add_belief(perc_rad643(high))"])
+!inspect_nuclear_plant643 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant643 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield644("G(add_belief(perc_rad644(low)))", ["add_belief(perc_rad644(low))", "add_belief(perc_rad644(high))"])
+!inspect_nuclear_plant644 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant644 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield645("G(add_belief(perc_rad645(low)))", ["add_belief(perc_rad645(low))", "add_belief(perc_rad645(high))"])
+!inspect_nuclear_plant645 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant645 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield646("G(add_belief(perc_rad646(low)))", ["add_belief(perc_rad646(low))", "add_belief(perc_rad646(high))"])
+!inspect_nuclear_plant646 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant646 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield647("G(add_belief(perc_rad647(low)))", ["add_belief(perc_rad647(low))", "add_belief(perc_rad647(high))"])
+!inspect_nuclear_plant647 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant647 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield648("G(add_belief(perc_rad648(low)))", ["add_belief(perc_rad648(low))", "add_belief(perc_rad648(high))"])
+!inspect_nuclear_plant648 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant648 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield649("G(add_belief(perc_rad649(low)))", ["add_belief(perc_rad649(low))", "add_belief(perc_rad649(high))"])
+!inspect_nuclear_plant649 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant649 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield650("G(add_belief(perc_rad650(low)))", ["add_belief(perc_rad650(low))", "add_belief(perc_rad650(high))"])
+!inspect_nuclear_plant650 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant650 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield651("G(add_belief(perc_rad651(low)))", ["add_belief(perc_rad651(low))", "add_belief(perc_rad651(high))"])
+!inspect_nuclear_plant651 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant651 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield652("G(add_belief(perc_rad652(low)))", ["add_belief(perc_rad652(low))", "add_belief(perc_rad652(high))"])
+!inspect_nuclear_plant652 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant652 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield653("G(add_belief(perc_rad653(low)))", ["add_belief(perc_rad653(low))", "add_belief(perc_rad653(high))"])
+!inspect_nuclear_plant653 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant653 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield654("G(add_belief(perc_rad654(low)))", ["add_belief(perc_rad654(low))", "add_belief(perc_rad654(high))"])
+!inspect_nuclear_plant654 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant654 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield655("G(add_belief(perc_rad655(low)))", ["add_belief(perc_rad655(low))", "add_belief(perc_rad655(high))"])
+!inspect_nuclear_plant655 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant655 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield656("G(add_belief(perc_rad656(low)))", ["add_belief(perc_rad656(low))", "add_belief(perc_rad656(high))"])
+!inspect_nuclear_plant656 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant656 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield657("G(add_belief(perc_rad657(low)))", ["add_belief(perc_rad657(low))", "add_belief(perc_rad657(high))"])
+!inspect_nuclear_plant657 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant657 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield658("G(add_belief(perc_rad658(low)))", ["add_belief(perc_rad658(low))", "add_belief(perc_rad658(high))"])
+!inspect_nuclear_plant658 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant658 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield659("G(add_belief(perc_rad659(low)))", ["add_belief(perc_rad659(low))", "add_belief(perc_rad659(high))"])
+!inspect_nuclear_plant659 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant659 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield660("G(add_belief(perc_rad660(low)))", ["add_belief(perc_rad660(low))", "add_belief(perc_rad660(high))"])
+!inspect_nuclear_plant660 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant660 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield661("G(add_belief(perc_rad661(low)))", ["add_belief(perc_rad661(low))", "add_belief(perc_rad661(high))"])
+!inspect_nuclear_plant661 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant661 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield662("G(add_belief(perc_rad662(low)))", ["add_belief(perc_rad662(low))", "add_belief(perc_rad662(high))"])
+!inspect_nuclear_plant662 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant662 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield663("G(add_belief(perc_rad663(low)))", ["add_belief(perc_rad663(low))", "add_belief(perc_rad663(high))"])
+!inspect_nuclear_plant663 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant663 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield664("G(add_belief(perc_rad664(low)))", ["add_belief(perc_rad664(low))", "add_belief(perc_rad664(high))"])
+!inspect_nuclear_plant664 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant664 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield665("G(add_belief(perc_rad665(low)))", ["add_belief(perc_rad665(low))", "add_belief(perc_rad665(high))"])
+!inspect_nuclear_plant665 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant665 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield666("G(add_belief(perc_rad666(low)))", ["add_belief(perc_rad666(low))", "add_belief(perc_rad666(high))"])
+!inspect_nuclear_plant666 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant666 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield667("G(add_belief(perc_rad667(low)))", ["add_belief(perc_rad667(low))", "add_belief(perc_rad667(high))"])
+!inspect_nuclear_plant667 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant667 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield668("G(add_belief(perc_rad668(low)))", ["add_belief(perc_rad668(low))", "add_belief(perc_rad668(high))"])
+!inspect_nuclear_plant668 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant668 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield669("G(add_belief(perc_rad669(low)))", ["add_belief(perc_rad669(low))", "add_belief(perc_rad669(high))"])
+!inspect_nuclear_plant669 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant669 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield670("G(add_belief(perc_rad670(low)))", ["add_belief(perc_rad670(low))", "add_belief(perc_rad670(high))"])
+!inspect_nuclear_plant670 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant670 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield671("G(add_belief(perc_rad671(low)))", ["add_belief(perc_rad671(low))", "add_belief(perc_rad671(high))"])
+!inspect_nuclear_plant671 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant671 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield672("G(add_belief(perc_rad672(low)))", ["add_belief(perc_rad672(low))", "add_belief(perc_rad672(high))"])
+!inspect_nuclear_plant672 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant672 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield673("G(add_belief(perc_rad673(low)))", ["add_belief(perc_rad673(low))", "add_belief(perc_rad673(high))"])
+!inspect_nuclear_plant673 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant673 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield674("G(add_belief(perc_rad674(low)))", ["add_belief(perc_rad674(low))", "add_belief(perc_rad674(high))"])
+!inspect_nuclear_plant674 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant674 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield675("G(add_belief(perc_rad675(low)))", ["add_belief(perc_rad675(low))", "add_belief(perc_rad675(high))"])
+!inspect_nuclear_plant675 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant675 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield676("G(add_belief(perc_rad676(low)))", ["add_belief(perc_rad676(low))", "add_belief(perc_rad676(high))"])
+!inspect_nuclear_plant676 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant676 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield677("G(add_belief(perc_rad677(low)))", ["add_belief(perc_rad677(low))", "add_belief(perc_rad677(high))"])
+!inspect_nuclear_plant677 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant677 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield678("G(add_belief(perc_rad678(low)))", ["add_belief(perc_rad678(low))", "add_belief(perc_rad678(high))"])
+!inspect_nuclear_plant678 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant678 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield679("G(add_belief(perc_rad679(low)))", ["add_belief(perc_rad679(low))", "add_belief(perc_rad679(high))"])
+!inspect_nuclear_plant679 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant679 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield680("G(add_belief(perc_rad680(low)))", ["add_belief(perc_rad680(low))", "add_belief(perc_rad680(high))"])
+!inspect_nuclear_plant680 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant680 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield681("G(add_belief(perc_rad681(low)))", ["add_belief(perc_rad681(low))", "add_belief(perc_rad681(high))"])
+!inspect_nuclear_plant681 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant681 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield682("G(add_belief(perc_rad682(low)))", ["add_belief(perc_rad682(low))", "add_belief(perc_rad682(high))"])
+!inspect_nuclear_plant682 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant682 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield683("G(add_belief(perc_rad683(low)))", ["add_belief(perc_rad683(low))", "add_belief(perc_rad683(high))"])
+!inspect_nuclear_plant683 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant683 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield684("G(add_belief(perc_rad684(low)))", ["add_belief(perc_rad684(low))", "add_belief(perc_rad684(high))"])
+!inspect_nuclear_plant684 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant684 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield685("G(add_belief(perc_rad685(low)))", ["add_belief(perc_rad685(low))", "add_belief(perc_rad685(high))"])
+!inspect_nuclear_plant685 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant685 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield686("G(add_belief(perc_rad686(low)))", ["add_belief(perc_rad686(low))", "add_belief(perc_rad686(high))"])
+!inspect_nuclear_plant686 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant686 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield687("G(add_belief(perc_rad687(low)))", ["add_belief(perc_rad687(low))", "add_belief(perc_rad687(high))"])
+!inspect_nuclear_plant687 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant687 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield688("G(add_belief(perc_rad688(low)))", ["add_belief(perc_rad688(low))", "add_belief(perc_rad688(high))"])
+!inspect_nuclear_plant688 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant688 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield689("G(add_belief(perc_rad689(low)))", ["add_belief(perc_rad689(low))", "add_belief(perc_rad689(high))"])
+!inspect_nuclear_plant689 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant689 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield690("G(add_belief(perc_rad690(low)))", ["add_belief(perc_rad690(low))", "add_belief(perc_rad690(high))"])
+!inspect_nuclear_plant690 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant690 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield691("G(add_belief(perc_rad691(low)))", ["add_belief(perc_rad691(low))", "add_belief(perc_rad691(high))"])
+!inspect_nuclear_plant691 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant691 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield692("G(add_belief(perc_rad692(low)))", ["add_belief(perc_rad692(low))", "add_belief(perc_rad692(high))"])
+!inspect_nuclear_plant692 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant692 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield693("G(add_belief(perc_rad693(low)))", ["add_belief(perc_rad693(low))", "add_belief(perc_rad693(high))"])
+!inspect_nuclear_plant693 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant693 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield694("G(add_belief(perc_rad694(low)))", ["add_belief(perc_rad694(low))", "add_belief(perc_rad694(high))"])
+!inspect_nuclear_plant694 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant694 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield695("G(add_belief(perc_rad695(low)))", ["add_belief(perc_rad695(low))", "add_belief(perc_rad695(high))"])
+!inspect_nuclear_plant695 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant695 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield696("G(add_belief(perc_rad696(low)))", ["add_belief(perc_rad696(low))", "add_belief(perc_rad696(high))"])
+!inspect_nuclear_plant696 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant696 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield697("G(add_belief(perc_rad697(low)))", ["add_belief(perc_rad697(low))", "add_belief(perc_rad697(high))"])
+!inspect_nuclear_plant697 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant697 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield698("G(add_belief(perc_rad698(low)))", ["add_belief(perc_rad698(low))", "add_belief(perc_rad698(high))"])
+!inspect_nuclear_plant698 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant698 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield699("G(add_belief(perc_rad699(low)))", ["add_belief(perc_rad699(low))", "add_belief(perc_rad699(high))"])
+!inspect_nuclear_plant699 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant699 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield700("G(add_belief(perc_rad700(low)))", ["add_belief(perc_rad700(low))", "add_belief(perc_rad700(high))"])
+!inspect_nuclear_plant700 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant700 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield701("G(add_belief(perc_rad701(low)))", ["add_belief(perc_rad701(low))", "add_belief(perc_rad701(high))"])
+!inspect_nuclear_plant701 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant701 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield702("G(add_belief(perc_rad702(low)))", ["add_belief(perc_rad702(low))", "add_belief(perc_rad702(high))"])
+!inspect_nuclear_plant702 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant702 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield703("G(add_belief(perc_rad703(low)))", ["add_belief(perc_rad703(low))", "add_belief(perc_rad703(high))"])
+!inspect_nuclear_plant703 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant703 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield704("G(add_belief(perc_rad704(low)))", ["add_belief(perc_rad704(low))", "add_belief(perc_rad704(high))"])
+!inspect_nuclear_plant704 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant704 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield705("G(add_belief(perc_rad705(low)))", ["add_belief(perc_rad705(low))", "add_belief(perc_rad705(high))"])
+!inspect_nuclear_plant705 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant705 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield706("G(add_belief(perc_rad706(low)))", ["add_belief(perc_rad706(low))", "add_belief(perc_rad706(high))"])
+!inspect_nuclear_plant706 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant706 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield707("G(add_belief(perc_rad707(low)))", ["add_belief(perc_rad707(low))", "add_belief(perc_rad707(high))"])
+!inspect_nuclear_plant707 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant707 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield708("G(add_belief(perc_rad708(low)))", ["add_belief(perc_rad708(low))", "add_belief(perc_rad708(high))"])
+!inspect_nuclear_plant708 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant708 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield709("G(add_belief(perc_rad709(low)))", ["add_belief(perc_rad709(low))", "add_belief(perc_rad709(high))"])
+!inspect_nuclear_plant709 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant709 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield710("G(add_belief(perc_rad710(low)))", ["add_belief(perc_rad710(low))", "add_belief(perc_rad710(high))"])
+!inspect_nuclear_plant710 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant710 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield711("G(add_belief(perc_rad711(low)))", ["add_belief(perc_rad711(low))", "add_belief(perc_rad711(high))"])
+!inspect_nuclear_plant711 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant711 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield712("G(add_belief(perc_rad712(low)))", ["add_belief(perc_rad712(low))", "add_belief(perc_rad712(high))"])
+!inspect_nuclear_plant712 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant712 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield713("G(add_belief(perc_rad713(low)))", ["add_belief(perc_rad713(low))", "add_belief(perc_rad713(high))"])
+!inspect_nuclear_plant713 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant713 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield714("G(add_belief(perc_rad714(low)))", ["add_belief(perc_rad714(low))", "add_belief(perc_rad714(high))"])
+!inspect_nuclear_plant714 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant714 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield715("G(add_belief(perc_rad715(low)))", ["add_belief(perc_rad715(low))", "add_belief(perc_rad715(high))"])
+!inspect_nuclear_plant715 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant715 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield716("G(add_belief(perc_rad716(low)))", ["add_belief(perc_rad716(low))", "add_belief(perc_rad716(high))"])
+!inspect_nuclear_plant716 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant716 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield717("G(add_belief(perc_rad717(low)))", ["add_belief(perc_rad717(low))", "add_belief(perc_rad717(high))"])
+!inspect_nuclear_plant717 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant717 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield718("G(add_belief(perc_rad718(low)))", ["add_belief(perc_rad718(low))", "add_belief(perc_rad718(high))"])
+!inspect_nuclear_plant718 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant718 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield719("G(add_belief(perc_rad719(low)))", ["add_belief(perc_rad719(low))", "add_belief(perc_rad719(high))"])
+!inspect_nuclear_plant719 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant719 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield720("G(add_belief(perc_rad720(low)))", ["add_belief(perc_rad720(low))", "add_belief(perc_rad720(high))"])
+!inspect_nuclear_plant720 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant720 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield721("G(add_belief(perc_rad721(low)))", ["add_belief(perc_rad721(low))", "add_belief(perc_rad721(high))"])
+!inspect_nuclear_plant721 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant721 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield722("G(add_belief(perc_rad722(low)))", ["add_belief(perc_rad722(low))", "add_belief(perc_rad722(high))"])
+!inspect_nuclear_plant722 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant722 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield723("G(add_belief(perc_rad723(low)))", ["add_belief(perc_rad723(low))", "add_belief(perc_rad723(high))"])
+!inspect_nuclear_plant723 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant723 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield724("G(add_belief(perc_rad724(low)))", ["add_belief(perc_rad724(low))", "add_belief(perc_rad724(high))"])
+!inspect_nuclear_plant724 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant724 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield725("G(add_belief(perc_rad725(low)))", ["add_belief(perc_rad725(low))", "add_belief(perc_rad725(high))"])
+!inspect_nuclear_plant725 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant725 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield726("G(add_belief(perc_rad726(low)))", ["add_belief(perc_rad726(low))", "add_belief(perc_rad726(high))"])
+!inspect_nuclear_plant726 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant726 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield727("G(add_belief(perc_rad727(low)))", ["add_belief(perc_rad727(low))", "add_belief(perc_rad727(high))"])
+!inspect_nuclear_plant727 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant727 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield728("G(add_belief(perc_rad728(low)))", ["add_belief(perc_rad728(low))", "add_belief(perc_rad728(high))"])
+!inspect_nuclear_plant728 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant728 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield729("G(add_belief(perc_rad729(low)))", ["add_belief(perc_rad729(low))", "add_belief(perc_rad729(high))"])
+!inspect_nuclear_plant729 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant729 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield730("G(add_belief(perc_rad730(low)))", ["add_belief(perc_rad730(low))", "add_belief(perc_rad730(high))"])
+!inspect_nuclear_plant730 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant730 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield731("G(add_belief(perc_rad731(low)))", ["add_belief(perc_rad731(low))", "add_belief(perc_rad731(high))"])
+!inspect_nuclear_plant731 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant731 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield732("G(add_belief(perc_rad732(low)))", ["add_belief(perc_rad732(low))", "add_belief(perc_rad732(high))"])
+!inspect_nuclear_plant732 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant732 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield733("G(add_belief(perc_rad733(low)))", ["add_belief(perc_rad733(low))", "add_belief(perc_rad733(high))"])
+!inspect_nuclear_plant733 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant733 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield734("G(add_belief(perc_rad734(low)))", ["add_belief(perc_rad734(low))", "add_belief(perc_rad734(high))"])
+!inspect_nuclear_plant734 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant734 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield735("G(add_belief(perc_rad735(low)))", ["add_belief(perc_rad735(low))", "add_belief(perc_rad735(high))"])
+!inspect_nuclear_plant735 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant735 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield736("G(add_belief(perc_rad736(low)))", ["add_belief(perc_rad736(low))", "add_belief(perc_rad736(high))"])
+!inspect_nuclear_plant736 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant736 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield737("G(add_belief(perc_rad737(low)))", ["add_belief(perc_rad737(low))", "add_belief(perc_rad737(high))"])
+!inspect_nuclear_plant737 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant737 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield738("G(add_belief(perc_rad738(low)))", ["add_belief(perc_rad738(low))", "add_belief(perc_rad738(high))"])
+!inspect_nuclear_plant738 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant738 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield739("G(add_belief(perc_rad739(low)))", ["add_belief(perc_rad739(low))", "add_belief(perc_rad739(high))"])
+!inspect_nuclear_plant739 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant739 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield740("G(add_belief(perc_rad740(low)))", ["add_belief(perc_rad740(low))", "add_belief(perc_rad740(high))"])
+!inspect_nuclear_plant740 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant740 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield741("G(add_belief(perc_rad741(low)))", ["add_belief(perc_rad741(low))", "add_belief(perc_rad741(high))"])
+!inspect_nuclear_plant741 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant741 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield742("G(add_belief(perc_rad742(low)))", ["add_belief(perc_rad742(low))", "add_belief(perc_rad742(high))"])
+!inspect_nuclear_plant742 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant742 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield743("G(add_belief(perc_rad743(low)))", ["add_belief(perc_rad743(low))", "add_belief(perc_rad743(high))"])
+!inspect_nuclear_plant743 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant743 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield744("G(add_belief(perc_rad744(low)))", ["add_belief(perc_rad744(low))", "add_belief(perc_rad744(high))"])
+!inspect_nuclear_plant744 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant744 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield745("G(add_belief(perc_rad745(low)))", ["add_belief(perc_rad745(low))", "add_belief(perc_rad745(high))"])
+!inspect_nuclear_plant745 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant745 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield746("G(add_belief(perc_rad746(low)))", ["add_belief(perc_rad746(low))", "add_belief(perc_rad746(high))"])
+!inspect_nuclear_plant746 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant746 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield747("G(add_belief(perc_rad747(low)))", ["add_belief(perc_rad747(low))", "add_belief(perc_rad747(high))"])
+!inspect_nuclear_plant747 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant747 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield748("G(add_belief(perc_rad748(low)))", ["add_belief(perc_rad748(low))", "add_belief(perc_rad748(high))"])
+!inspect_nuclear_plant748 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant748 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield749("G(add_belief(perc_rad749(low)))", ["add_belief(perc_rad749(low))", "add_belief(perc_rad749(high))"])
+!inspect_nuclear_plant749 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant749 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield750("G(add_belief(perc_rad750(low)))", ["add_belief(perc_rad750(low))", "add_belief(perc_rad750(high))"])
+!inspect_nuclear_plant750 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant750 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield751("G(add_belief(perc_rad751(low)))", ["add_belief(perc_rad751(low))", "add_belief(perc_rad751(high))"])
+!inspect_nuclear_plant751 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant751 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield752("G(add_belief(perc_rad752(low)))", ["add_belief(perc_rad752(low))", "add_belief(perc_rad752(high))"])
+!inspect_nuclear_plant752 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant752 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield753("G(add_belief(perc_rad753(low)))", ["add_belief(perc_rad753(low))", "add_belief(perc_rad753(high))"])
+!inspect_nuclear_plant753 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant753 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield754("G(add_belief(perc_rad754(low)))", ["add_belief(perc_rad754(low))", "add_belief(perc_rad754(high))"])
+!inspect_nuclear_plant754 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant754 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield755("G(add_belief(perc_rad755(low)))", ["add_belief(perc_rad755(low))", "add_belief(perc_rad755(high))"])
+!inspect_nuclear_plant755 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant755 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield756("G(add_belief(perc_rad756(low)))", ["add_belief(perc_rad756(low))", "add_belief(perc_rad756(high))"])
+!inspect_nuclear_plant756 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant756 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield757("G(add_belief(perc_rad757(low)))", ["add_belief(perc_rad757(low))", "add_belief(perc_rad757(high))"])
+!inspect_nuclear_plant757 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant757 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield758("G(add_belief(perc_rad758(low)))", ["add_belief(perc_rad758(low))", "add_belief(perc_rad758(high))"])
+!inspect_nuclear_plant758 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant758 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield759("G(add_belief(perc_rad759(low)))", ["add_belief(perc_rad759(low))", "add_belief(perc_rad759(high))"])
+!inspect_nuclear_plant759 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant759 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield760("G(add_belief(perc_rad760(low)))", ["add_belief(perc_rad760(low))", "add_belief(perc_rad760(high))"])
+!inspect_nuclear_plant760 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant760 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield761("G(add_belief(perc_rad761(low)))", ["add_belief(perc_rad761(low))", "add_belief(perc_rad761(high))"])
+!inspect_nuclear_plant761 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant761 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield762("G(add_belief(perc_rad762(low)))", ["add_belief(perc_rad762(low))", "add_belief(perc_rad762(high))"])
+!inspect_nuclear_plant762 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant762 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield763("G(add_belief(perc_rad763(low)))", ["add_belief(perc_rad763(low))", "add_belief(perc_rad763(high))"])
+!inspect_nuclear_plant763 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant763 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield764("G(add_belief(perc_rad764(low)))", ["add_belief(perc_rad764(low))", "add_belief(perc_rad764(high))"])
+!inspect_nuclear_plant764 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant764 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield765("G(add_belief(perc_rad765(low)))", ["add_belief(perc_rad765(low))", "add_belief(perc_rad765(high))"])
+!inspect_nuclear_plant765 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant765 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield766("G(add_belief(perc_rad766(low)))", ["add_belief(perc_rad766(low))", "add_belief(perc_rad766(high))"])
+!inspect_nuclear_plant766 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant766 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield767("G(add_belief(perc_rad767(low)))", ["add_belief(perc_rad767(low))", "add_belief(perc_rad767(high))"])
+!inspect_nuclear_plant767 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant767 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield768("G(add_belief(perc_rad768(low)))", ["add_belief(perc_rad768(low))", "add_belief(perc_rad768(high))"])
+!inspect_nuclear_plant768 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant768 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield769("G(add_belief(perc_rad769(low)))", ["add_belief(perc_rad769(low))", "add_belief(perc_rad769(high))"])
+!inspect_nuclear_plant769 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant769 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield770("G(add_belief(perc_rad770(low)))", ["add_belief(perc_rad770(low))", "add_belief(perc_rad770(high))"])
+!inspect_nuclear_plant770 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant770 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield771("G(add_belief(perc_rad771(low)))", ["add_belief(perc_rad771(low))", "add_belief(perc_rad771(high))"])
+!inspect_nuclear_plant771 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant771 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield772("G(add_belief(perc_rad772(low)))", ["add_belief(perc_rad772(low))", "add_belief(perc_rad772(high))"])
+!inspect_nuclear_plant772 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant772 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield773("G(add_belief(perc_rad773(low)))", ["add_belief(perc_rad773(low))", "add_belief(perc_rad773(high))"])
+!inspect_nuclear_plant773 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant773 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield774("G(add_belief(perc_rad774(low)))", ["add_belief(perc_rad774(low))", "add_belief(perc_rad774(high))"])
+!inspect_nuclear_plant774 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant774 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield775("G(add_belief(perc_rad775(low)))", ["add_belief(perc_rad775(low))", "add_belief(perc_rad775(high))"])
+!inspect_nuclear_plant775 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant775 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield776("G(add_belief(perc_rad776(low)))", ["add_belief(perc_rad776(low))", "add_belief(perc_rad776(high))"])
+!inspect_nuclear_plant776 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant776 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield777("G(add_belief(perc_rad777(low)))", ["add_belief(perc_rad777(low))", "add_belief(perc_rad777(high))"])
+!inspect_nuclear_plant777 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant777 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield778("G(add_belief(perc_rad778(low)))", ["add_belief(perc_rad778(low))", "add_belief(perc_rad778(high))"])
+!inspect_nuclear_plant778 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant778 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield779("G(add_belief(perc_rad779(low)))", ["add_belief(perc_rad779(low))", "add_belief(perc_rad779(high))"])
+!inspect_nuclear_plant779 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant779 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield780("G(add_belief(perc_rad780(low)))", ["add_belief(perc_rad780(low))", "add_belief(perc_rad780(high))"])
+!inspect_nuclear_plant780 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant780 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield781("G(add_belief(perc_rad781(low)))", ["add_belief(perc_rad781(low))", "add_belief(perc_rad781(high))"])
+!inspect_nuclear_plant781 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant781 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield782("G(add_belief(perc_rad782(low)))", ["add_belief(perc_rad782(low))", "add_belief(perc_rad782(high))"])
+!inspect_nuclear_plant782 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant782 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield783("G(add_belief(perc_rad783(low)))", ["add_belief(perc_rad783(low))", "add_belief(perc_rad783(high))"])
+!inspect_nuclear_plant783 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant783 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield784("G(add_belief(perc_rad784(low)))", ["add_belief(perc_rad784(low))", "add_belief(perc_rad784(high))"])
+!inspect_nuclear_plant784 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant784 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield785("G(add_belief(perc_rad785(low)))", ["add_belief(perc_rad785(low))", "add_belief(perc_rad785(high))"])
+!inspect_nuclear_plant785 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant785 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield786("G(add_belief(perc_rad786(low)))", ["add_belief(perc_rad786(low))", "add_belief(perc_rad786(high))"])
+!inspect_nuclear_plant786 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant786 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield787("G(add_belief(perc_rad787(low)))", ["add_belief(perc_rad787(low))", "add_belief(perc_rad787(high))"])
+!inspect_nuclear_plant787 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant787 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield788("G(add_belief(perc_rad788(low)))", ["add_belief(perc_rad788(low))", "add_belief(perc_rad788(high))"])
+!inspect_nuclear_plant788 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant788 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield789("G(add_belief(perc_rad789(low)))", ["add_belief(perc_rad789(low))", "add_belief(perc_rad789(high))"])
+!inspect_nuclear_plant789 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant789 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield790("G(add_belief(perc_rad790(low)))", ["add_belief(perc_rad790(low))", "add_belief(perc_rad790(high))"])
+!inspect_nuclear_plant790 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant790 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield791("G(add_belief(perc_rad791(low)))", ["add_belief(perc_rad791(low))", "add_belief(perc_rad791(high))"])
+!inspect_nuclear_plant791 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant791 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield792("G(add_belief(perc_rad792(low)))", ["add_belief(perc_rad792(low))", "add_belief(perc_rad792(high))"])
+!inspect_nuclear_plant792 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant792 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield793("G(add_belief(perc_rad793(low)))", ["add_belief(perc_rad793(low))", "add_belief(perc_rad793(high))"])
+!inspect_nuclear_plant793 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant793 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield794("G(add_belief(perc_rad794(low)))", ["add_belief(perc_rad794(low))", "add_belief(perc_rad794(high))"])
+!inspect_nuclear_plant794 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant794 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield795("G(add_belief(perc_rad795(low)))", ["add_belief(perc_rad795(low))", "add_belief(perc_rad795(high))"])
+!inspect_nuclear_plant795 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant795 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield796("G(add_belief(perc_rad796(low)))", ["add_belief(perc_rad796(low))", "add_belief(perc_rad796(high))"])
+!inspect_nuclear_plant796 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant796 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield797("G(add_belief(perc_rad797(low)))", ["add_belief(perc_rad797(low))", "add_belief(perc_rad797(high))"])
+!inspect_nuclear_plant797 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant797 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield798("G(add_belief(perc_rad798(low)))", ["add_belief(perc_rad798(low))", "add_belief(perc_rad798(high))"])
+!inspect_nuclear_plant798 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant798 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield799("G(add_belief(perc_rad799(low)))", ["add_belief(perc_rad799(low))", "add_belief(perc_rad799(high))"])
+!inspect_nuclear_plant799 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant799 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield800("G(add_belief(perc_rad800(low)))", ["add_belief(perc_rad800(low))", "add_belief(perc_rad800(high))"])
+!inspect_nuclear_plant800 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant800 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield801("G(add_belief(perc_rad801(low)))", ["add_belief(perc_rad801(low))", "add_belief(perc_rad801(high))"])
+!inspect_nuclear_plant801 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant801 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield802("G(add_belief(perc_rad802(low)))", ["add_belief(perc_rad802(low))", "add_belief(perc_rad802(high))"])
+!inspect_nuclear_plant802 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant802 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield803("G(add_belief(perc_rad803(low)))", ["add_belief(perc_rad803(low))", "add_belief(perc_rad803(high))"])
+!inspect_nuclear_plant803 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant803 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield804("G(add_belief(perc_rad804(low)))", ["add_belief(perc_rad804(low))", "add_belief(perc_rad804(high))"])
+!inspect_nuclear_plant804 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant804 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield805("G(add_belief(perc_rad805(low)))", ["add_belief(perc_rad805(low))", "add_belief(perc_rad805(high))"])
+!inspect_nuclear_plant805 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant805 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield806("G(add_belief(perc_rad806(low)))", ["add_belief(perc_rad806(low))", "add_belief(perc_rad806(high))"])
+!inspect_nuclear_plant806 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant806 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield807("G(add_belief(perc_rad807(low)))", ["add_belief(perc_rad807(low))", "add_belief(perc_rad807(high))"])
+!inspect_nuclear_plant807 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant807 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield808("G(add_belief(perc_rad808(low)))", ["add_belief(perc_rad808(low))", "add_belief(perc_rad808(high))"])
+!inspect_nuclear_plant808 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant808 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield809("G(add_belief(perc_rad809(low)))", ["add_belief(perc_rad809(low))", "add_belief(perc_rad809(high))"])
+!inspect_nuclear_plant809 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant809 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield810("G(add_belief(perc_rad810(low)))", ["add_belief(perc_rad810(low))", "add_belief(perc_rad810(high))"])
+!inspect_nuclear_plant810 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant810 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield811("G(add_belief(perc_rad811(low)))", ["add_belief(perc_rad811(low))", "add_belief(perc_rad811(high))"])
+!inspect_nuclear_plant811 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant811 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield812("G(add_belief(perc_rad812(low)))", ["add_belief(perc_rad812(low))", "add_belief(perc_rad812(high))"])
+!inspect_nuclear_plant812 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant812 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield813("G(add_belief(perc_rad813(low)))", ["add_belief(perc_rad813(low))", "add_belief(perc_rad813(high))"])
+!inspect_nuclear_plant813 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant813 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield814("G(add_belief(perc_rad814(low)))", ["add_belief(perc_rad814(low))", "add_belief(perc_rad814(high))"])
+!inspect_nuclear_plant814 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant814 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield815("G(add_belief(perc_rad815(low)))", ["add_belief(perc_rad815(low))", "add_belief(perc_rad815(high))"])
+!inspect_nuclear_plant815 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant815 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield816("G(add_belief(perc_rad816(low)))", ["add_belief(perc_rad816(low))", "add_belief(perc_rad816(high))"])
+!inspect_nuclear_plant816 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant816 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield817("G(add_belief(perc_rad817(low)))", ["add_belief(perc_rad817(low))", "add_belief(perc_rad817(high))"])
+!inspect_nuclear_plant817 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant817 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield818("G(add_belief(perc_rad818(low)))", ["add_belief(perc_rad818(low))", "add_belief(perc_rad818(high))"])
+!inspect_nuclear_plant818 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant818 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield819("G(add_belief(perc_rad819(low)))", ["add_belief(perc_rad819(low))", "add_belief(perc_rad819(high))"])
+!inspect_nuclear_plant819 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant819 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield820("G(add_belief(perc_rad820(low)))", ["add_belief(perc_rad820(low))", "add_belief(perc_rad820(high))"])
+!inspect_nuclear_plant820 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant820 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield821("G(add_belief(perc_rad821(low)))", ["add_belief(perc_rad821(low))", "add_belief(perc_rad821(high))"])
+!inspect_nuclear_plant821 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant821 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield822("G(add_belief(perc_rad822(low)))", ["add_belief(perc_rad822(low))", "add_belief(perc_rad822(high))"])
+!inspect_nuclear_plant822 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant822 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield823("G(add_belief(perc_rad823(low)))", ["add_belief(perc_rad823(low))", "add_belief(perc_rad823(high))"])
+!inspect_nuclear_plant823 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant823 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield824("G(add_belief(perc_rad824(low)))", ["add_belief(perc_rad824(low))", "add_belief(perc_rad824(high))"])
+!inspect_nuclear_plant824 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant824 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield825("G(add_belief(perc_rad825(low)))", ["add_belief(perc_rad825(low))", "add_belief(perc_rad825(high))"])
+!inspect_nuclear_plant825 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant825 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield826("G(add_belief(perc_rad826(low)))", ["add_belief(perc_rad826(low))", "add_belief(perc_rad826(high))"])
+!inspect_nuclear_plant826 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant826 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield827("G(add_belief(perc_rad827(low)))", ["add_belief(perc_rad827(low))", "add_belief(perc_rad827(high))"])
+!inspect_nuclear_plant827 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant827 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield828("G(add_belief(perc_rad828(low)))", ["add_belief(perc_rad828(low))", "add_belief(perc_rad828(high))"])
+!inspect_nuclear_plant828 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant828 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield829("G(add_belief(perc_rad829(low)))", ["add_belief(perc_rad829(low))", "add_belief(perc_rad829(high))"])
+!inspect_nuclear_plant829 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant829 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield830("G(add_belief(perc_rad830(low)))", ["add_belief(perc_rad830(low))", "add_belief(perc_rad830(high))"])
+!inspect_nuclear_plant830 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant830 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield831("G(add_belief(perc_rad831(low)))", ["add_belief(perc_rad831(low))", "add_belief(perc_rad831(high))"])
+!inspect_nuclear_plant831 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant831 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield832("G(add_belief(perc_rad832(low)))", ["add_belief(perc_rad832(low))", "add_belief(perc_rad832(high))"])
+!inspect_nuclear_plant832 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant832 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield833("G(add_belief(perc_rad833(low)))", ["add_belief(perc_rad833(low))", "add_belief(perc_rad833(high))"])
+!inspect_nuclear_plant833 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant833 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield834("G(add_belief(perc_rad834(low)))", ["add_belief(perc_rad834(low))", "add_belief(perc_rad834(high))"])
+!inspect_nuclear_plant834 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant834 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield835("G(add_belief(perc_rad835(low)))", ["add_belief(perc_rad835(low))", "add_belief(perc_rad835(high))"])
+!inspect_nuclear_plant835 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant835 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield836("G(add_belief(perc_rad836(low)))", ["add_belief(perc_rad836(low))", "add_belief(perc_rad836(high))"])
+!inspect_nuclear_plant836 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant836 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield837("G(add_belief(perc_rad837(low)))", ["add_belief(perc_rad837(low))", "add_belief(perc_rad837(high))"])
+!inspect_nuclear_plant837 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant837 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield838("G(add_belief(perc_rad838(low)))", ["add_belief(perc_rad838(low))", "add_belief(perc_rad838(high))"])
+!inspect_nuclear_plant838 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant838 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield839("G(add_belief(perc_rad839(low)))", ["add_belief(perc_rad839(low))", "add_belief(perc_rad839(high))"])
+!inspect_nuclear_plant839 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant839 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield840("G(add_belief(perc_rad840(low)))", ["add_belief(perc_rad840(low))", "add_belief(perc_rad840(high))"])
+!inspect_nuclear_plant840 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant840 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield841("G(add_belief(perc_rad841(low)))", ["add_belief(perc_rad841(low))", "add_belief(perc_rad841(high))"])
+!inspect_nuclear_plant841 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant841 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield842("G(add_belief(perc_rad842(low)))", ["add_belief(perc_rad842(low))", "add_belief(perc_rad842(high))"])
+!inspect_nuclear_plant842 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant842 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield843("G(add_belief(perc_rad843(low)))", ["add_belief(perc_rad843(low))", "add_belief(perc_rad843(high))"])
+!inspect_nuclear_plant843 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant843 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield844("G(add_belief(perc_rad844(low)))", ["add_belief(perc_rad844(low))", "add_belief(perc_rad844(high))"])
+!inspect_nuclear_plant844 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant844 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield845("G(add_belief(perc_rad845(low)))", ["add_belief(perc_rad845(low))", "add_belief(perc_rad845(high))"])
+!inspect_nuclear_plant845 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant845 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield846("G(add_belief(perc_rad846(low)))", ["add_belief(perc_rad846(low))", "add_belief(perc_rad846(high))"])
+!inspect_nuclear_plant846 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant846 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield847("G(add_belief(perc_rad847(low)))", ["add_belief(perc_rad847(low))", "add_belief(perc_rad847(high))"])
+!inspect_nuclear_plant847 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant847 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield848("G(add_belief(perc_rad848(low)))", ["add_belief(perc_rad848(low))", "add_belief(perc_rad848(high))"])
+!inspect_nuclear_plant848 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant848 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield849("G(add_belief(perc_rad849(low)))", ["add_belief(perc_rad849(low))", "add_belief(perc_rad849(high))"])
+!inspect_nuclear_plant849 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant849 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield850("G(add_belief(perc_rad850(low)))", ["add_belief(perc_rad850(low))", "add_belief(perc_rad850(high))"])
+!inspect_nuclear_plant850 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant850 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield851("G(add_belief(perc_rad851(low)))", ["add_belief(perc_rad851(low))", "add_belief(perc_rad851(high))"])
+!inspect_nuclear_plant851 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant851 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield852("G(add_belief(perc_rad852(low)))", ["add_belief(perc_rad852(low))", "add_belief(perc_rad852(high))"])
+!inspect_nuclear_plant852 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant852 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield853("G(add_belief(perc_rad853(low)))", ["add_belief(perc_rad853(low))", "add_belief(perc_rad853(high))"])
+!inspect_nuclear_plant853 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant853 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield854("G(add_belief(perc_rad854(low)))", ["add_belief(perc_rad854(low))", "add_belief(perc_rad854(high))"])
+!inspect_nuclear_plant854 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant854 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield855("G(add_belief(perc_rad855(low)))", ["add_belief(perc_rad855(low))", "add_belief(perc_rad855(high))"])
+!inspect_nuclear_plant855 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant855 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield856("G(add_belief(perc_rad856(low)))", ["add_belief(perc_rad856(low))", "add_belief(perc_rad856(high))"])
+!inspect_nuclear_plant856 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant856 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield857("G(add_belief(perc_rad857(low)))", ["add_belief(perc_rad857(low))", "add_belief(perc_rad857(high))"])
+!inspect_nuclear_plant857 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant857 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield858("G(add_belief(perc_rad858(low)))", ["add_belief(perc_rad858(low))", "add_belief(perc_rad858(high))"])
+!inspect_nuclear_plant858 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant858 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield859("G(add_belief(perc_rad859(low)))", ["add_belief(perc_rad859(low))", "add_belief(perc_rad859(high))"])
+!inspect_nuclear_plant859 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant859 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield860("G(add_belief(perc_rad860(low)))", ["add_belief(perc_rad860(low))", "add_belief(perc_rad860(high))"])
+!inspect_nuclear_plant860 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant860 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield861("G(add_belief(perc_rad861(low)))", ["add_belief(perc_rad861(low))", "add_belief(perc_rad861(high))"])
+!inspect_nuclear_plant861 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant861 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield862("G(add_belief(perc_rad862(low)))", ["add_belief(perc_rad862(low))", "add_belief(perc_rad862(high))"])
+!inspect_nuclear_plant862 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant862 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield863("G(add_belief(perc_rad863(low)))", ["add_belief(perc_rad863(low))", "add_belief(perc_rad863(high))"])
+!inspect_nuclear_plant863 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant863 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield864("G(add_belief(perc_rad864(low)))", ["add_belief(perc_rad864(low))", "add_belief(perc_rad864(high))"])
+!inspect_nuclear_plant864 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant864 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield865("G(add_belief(perc_rad865(low)))", ["add_belief(perc_rad865(low))", "add_belief(perc_rad865(high))"])
+!inspect_nuclear_plant865 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant865 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield866("G(add_belief(perc_rad866(low)))", ["add_belief(perc_rad866(low))", "add_belief(perc_rad866(high))"])
+!inspect_nuclear_plant866 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant866 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield867("G(add_belief(perc_rad867(low)))", ["add_belief(perc_rad867(low))", "add_belief(perc_rad867(high))"])
+!inspect_nuclear_plant867 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant867 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield868("G(add_belief(perc_rad868(low)))", ["add_belief(perc_rad868(low))", "add_belief(perc_rad868(high))"])
+!inspect_nuclear_plant868 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant868 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield869("G(add_belief(perc_rad869(low)))", ["add_belief(perc_rad869(low))", "add_belief(perc_rad869(high))"])
+!inspect_nuclear_plant869 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant869 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield870("G(add_belief(perc_rad870(low)))", ["add_belief(perc_rad870(low))", "add_belief(perc_rad870(high))"])
+!inspect_nuclear_plant870 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant870 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield871("G(add_belief(perc_rad871(low)))", ["add_belief(perc_rad871(low))", "add_belief(perc_rad871(high))"])
+!inspect_nuclear_plant871 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant871 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield872("G(add_belief(perc_rad872(low)))", ["add_belief(perc_rad872(low))", "add_belief(perc_rad872(high))"])
+!inspect_nuclear_plant872 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant872 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield873("G(add_belief(perc_rad873(low)))", ["add_belief(perc_rad873(low))", "add_belief(perc_rad873(high))"])
+!inspect_nuclear_plant873 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant873 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield874("G(add_belief(perc_rad874(low)))", ["add_belief(perc_rad874(low))", "add_belief(perc_rad874(high))"])
+!inspect_nuclear_plant874 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant874 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield875("G(add_belief(perc_rad875(low)))", ["add_belief(perc_rad875(low))", "add_belief(perc_rad875(high))"])
+!inspect_nuclear_plant875 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant875 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield876("G(add_belief(perc_rad876(low)))", ["add_belief(perc_rad876(low))", "add_belief(perc_rad876(high))"])
+!inspect_nuclear_plant876 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant876 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield877("G(add_belief(perc_rad877(low)))", ["add_belief(perc_rad877(low))", "add_belief(perc_rad877(high))"])
+!inspect_nuclear_plant877 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant877 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield878("G(add_belief(perc_rad878(low)))", ["add_belief(perc_rad878(low))", "add_belief(perc_rad878(high))"])
+!inspect_nuclear_plant878 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant878 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield879("G(add_belief(perc_rad879(low)))", ["add_belief(perc_rad879(low))", "add_belief(perc_rad879(high))"])
+!inspect_nuclear_plant879 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant879 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield880("G(add_belief(perc_rad880(low)))", ["add_belief(perc_rad880(low))", "add_belief(perc_rad880(high))"])
+!inspect_nuclear_plant880 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant880 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield881("G(add_belief(perc_rad881(low)))", ["add_belief(perc_rad881(low))", "add_belief(perc_rad881(high))"])
+!inspect_nuclear_plant881 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant881 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield882("G(add_belief(perc_rad882(low)))", ["add_belief(perc_rad882(low))", "add_belief(perc_rad882(high))"])
+!inspect_nuclear_plant882 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant882 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield883("G(add_belief(perc_rad883(low)))", ["add_belief(perc_rad883(low))", "add_belief(perc_rad883(high))"])
+!inspect_nuclear_plant883 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant883 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield884("G(add_belief(perc_rad884(low)))", ["add_belief(perc_rad884(low))", "add_belief(perc_rad884(high))"])
+!inspect_nuclear_plant884 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant884 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield885("G(add_belief(perc_rad885(low)))", ["add_belief(perc_rad885(low))", "add_belief(perc_rad885(high))"])
+!inspect_nuclear_plant885 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant885 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield886("G(add_belief(perc_rad886(low)))", ["add_belief(perc_rad886(low))", "add_belief(perc_rad886(high))"])
+!inspect_nuclear_plant886 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant886 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield887("G(add_belief(perc_rad887(low)))", ["add_belief(perc_rad887(low))", "add_belief(perc_rad887(high))"])
+!inspect_nuclear_plant887 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant887 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield888("G(add_belief(perc_rad888(low)))", ["add_belief(perc_rad888(low))", "add_belief(perc_rad888(high))"])
+!inspect_nuclear_plant888 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant888 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield889("G(add_belief(perc_rad889(low)))", ["add_belief(perc_rad889(low))", "add_belief(perc_rad889(high))"])
+!inspect_nuclear_plant889 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant889 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield890("G(add_belief(perc_rad890(low)))", ["add_belief(perc_rad890(low))", "add_belief(perc_rad890(high))"])
+!inspect_nuclear_plant890 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant890 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield891("G(add_belief(perc_rad891(low)))", ["add_belief(perc_rad891(low))", "add_belief(perc_rad891(high))"])
+!inspect_nuclear_plant891 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant891 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield892("G(add_belief(perc_rad892(low)))", ["add_belief(perc_rad892(low))", "add_belief(perc_rad892(high))"])
+!inspect_nuclear_plant892 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant892 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield893("G(add_belief(perc_rad893(low)))", ["add_belief(perc_rad893(low))", "add_belief(perc_rad893(high))"])
+!inspect_nuclear_plant893 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant893 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield894("G(add_belief(perc_rad894(low)))", ["add_belief(perc_rad894(low))", "add_belief(perc_rad894(high))"])
+!inspect_nuclear_plant894 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant894 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield895("G(add_belief(perc_rad895(low)))", ["add_belief(perc_rad895(low))", "add_belief(perc_rad895(high))"])
+!inspect_nuclear_plant895 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant895 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield896("G(add_belief(perc_rad896(low)))", ["add_belief(perc_rad896(low))", "add_belief(perc_rad896(high))"])
+!inspect_nuclear_plant896 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant896 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield897("G(add_belief(perc_rad897(low)))", ["add_belief(perc_rad897(low))", "add_belief(perc_rad897(high))"])
+!inspect_nuclear_plant897 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant897 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield898("G(add_belief(perc_rad898(low)))", ["add_belief(perc_rad898(low))", "add_belief(perc_rad898(high))"])
+!inspect_nuclear_plant898 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant898 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield899("G(add_belief(perc_rad899(low)))", ["add_belief(perc_rad899(low))", "add_belief(perc_rad899(high))"])
+!inspect_nuclear_plant899 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant899 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield900("G(add_belief(perc_rad900(low)))", ["add_belief(perc_rad900(low))", "add_belief(perc_rad900(high))"])
+!inspect_nuclear_plant900 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant900 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield901("G(add_belief(perc_rad901(low)))", ["add_belief(perc_rad901(low))", "add_belief(perc_rad901(high))"])
+!inspect_nuclear_plant901 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant901 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield902("G(add_belief(perc_rad902(low)))", ["add_belief(perc_rad902(low))", "add_belief(perc_rad902(high))"])
+!inspect_nuclear_plant902 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant902 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield903("G(add_belief(perc_rad903(low)))", ["add_belief(perc_rad903(low))", "add_belief(perc_rad903(high))"])
+!inspect_nuclear_plant903 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant903 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield904("G(add_belief(perc_rad904(low)))", ["add_belief(perc_rad904(low))", "add_belief(perc_rad904(high))"])
+!inspect_nuclear_plant904 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant904 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield905("G(add_belief(perc_rad905(low)))", ["add_belief(perc_rad905(low))", "add_belief(perc_rad905(high))"])
+!inspect_nuclear_plant905 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant905 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield906("G(add_belief(perc_rad906(low)))", ["add_belief(perc_rad906(low))", "add_belief(perc_rad906(high))"])
+!inspect_nuclear_plant906 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant906 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield907("G(add_belief(perc_rad907(low)))", ["add_belief(perc_rad907(low))", "add_belief(perc_rad907(high))"])
+!inspect_nuclear_plant907 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant907 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield908("G(add_belief(perc_rad908(low)))", ["add_belief(perc_rad908(low))", "add_belief(perc_rad908(high))"])
+!inspect_nuclear_plant908 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant908 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield909("G(add_belief(perc_rad909(low)))", ["add_belief(perc_rad909(low))", "add_belief(perc_rad909(high))"])
+!inspect_nuclear_plant909 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant909 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield910("G(add_belief(perc_rad910(low)))", ["add_belief(perc_rad910(low))", "add_belief(perc_rad910(high))"])
+!inspect_nuclear_plant910 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant910 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield911("G(add_belief(perc_rad911(low)))", ["add_belief(perc_rad911(low))", "add_belief(perc_rad911(high))"])
+!inspect_nuclear_plant911 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant911 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield912("G(add_belief(perc_rad912(low)))", ["add_belief(perc_rad912(low))", "add_belief(perc_rad912(high))"])
+!inspect_nuclear_plant912 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant912 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield913("G(add_belief(perc_rad913(low)))", ["add_belief(perc_rad913(low))", "add_belief(perc_rad913(high))"])
+!inspect_nuclear_plant913 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant913 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield914("G(add_belief(perc_rad914(low)))", ["add_belief(perc_rad914(low))", "add_belief(perc_rad914(high))"])
+!inspect_nuclear_plant914 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant914 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield915("G(add_belief(perc_rad915(low)))", ["add_belief(perc_rad915(low))", "add_belief(perc_rad915(high))"])
+!inspect_nuclear_plant915 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant915 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield916("G(add_belief(perc_rad916(low)))", ["add_belief(perc_rad916(low))", "add_belief(perc_rad916(high))"])
+!inspect_nuclear_plant916 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant916 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield917("G(add_belief(perc_rad917(low)))", ["add_belief(perc_rad917(low))", "add_belief(perc_rad917(high))"])
+!inspect_nuclear_plant917 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant917 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield918("G(add_belief(perc_rad918(low)))", ["add_belief(perc_rad918(low))", "add_belief(perc_rad918(high))"])
+!inspect_nuclear_plant918 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant918 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield919("G(add_belief(perc_rad919(low)))", ["add_belief(perc_rad919(low))", "add_belief(perc_rad919(high))"])
+!inspect_nuclear_plant919 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant919 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield920("G(add_belief(perc_rad920(low)))", ["add_belief(perc_rad920(low))", "add_belief(perc_rad920(high))"])
+!inspect_nuclear_plant920 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant920 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield921("G(add_belief(perc_rad921(low)))", ["add_belief(perc_rad921(low))", "add_belief(perc_rad921(high))"])
+!inspect_nuclear_plant921 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant921 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield922("G(add_belief(perc_rad922(low)))", ["add_belief(perc_rad922(low))", "add_belief(perc_rad922(high))"])
+!inspect_nuclear_plant922 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant922 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield923("G(add_belief(perc_rad923(low)))", ["add_belief(perc_rad923(low))", "add_belief(perc_rad923(high))"])
+!inspect_nuclear_plant923 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant923 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield924("G(add_belief(perc_rad924(low)))", ["add_belief(perc_rad924(low))", "add_belief(perc_rad924(high))"])
+!inspect_nuclear_plant924 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant924 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield925("G(add_belief(perc_rad925(low)))", ["add_belief(perc_rad925(low))", "add_belief(perc_rad925(high))"])
+!inspect_nuclear_plant925 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant925 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield926("G(add_belief(perc_rad926(low)))", ["add_belief(perc_rad926(low))", "add_belief(perc_rad926(high))"])
+!inspect_nuclear_plant926 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant926 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield927("G(add_belief(perc_rad927(low)))", ["add_belief(perc_rad927(low))", "add_belief(perc_rad927(high))"])
+!inspect_nuclear_plant927 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant927 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield928("G(add_belief(perc_rad928(low)))", ["add_belief(perc_rad928(low))", "add_belief(perc_rad928(high))"])
+!inspect_nuclear_plant928 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant928 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield929("G(add_belief(perc_rad929(low)))", ["add_belief(perc_rad929(low))", "add_belief(perc_rad929(high))"])
+!inspect_nuclear_plant929 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant929 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield930("G(add_belief(perc_rad930(low)))", ["add_belief(perc_rad930(low))", "add_belief(perc_rad930(high))"])
+!inspect_nuclear_plant930 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant930 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield931("G(add_belief(perc_rad931(low)))", ["add_belief(perc_rad931(low))", "add_belief(perc_rad931(high))"])
+!inspect_nuclear_plant931 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant931 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield932("G(add_belief(perc_rad932(low)))", ["add_belief(perc_rad932(low))", "add_belief(perc_rad932(high))"])
+!inspect_nuclear_plant932 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant932 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield933("G(add_belief(perc_rad933(low)))", ["add_belief(perc_rad933(low))", "add_belief(perc_rad933(high))"])
+!inspect_nuclear_plant933 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant933 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield934("G(add_belief(perc_rad934(low)))", ["add_belief(perc_rad934(low))", "add_belief(perc_rad934(high))"])
+!inspect_nuclear_plant934 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant934 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield935("G(add_belief(perc_rad935(low)))", ["add_belief(perc_rad935(low))", "add_belief(perc_rad935(high))"])
+!inspect_nuclear_plant935 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant935 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield936("G(add_belief(perc_rad936(low)))", ["add_belief(perc_rad936(low))", "add_belief(perc_rad936(high))"])
+!inspect_nuclear_plant936 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant936 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield937("G(add_belief(perc_rad937(low)))", ["add_belief(perc_rad937(low))", "add_belief(perc_rad937(high))"])
+!inspect_nuclear_plant937 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant937 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield938("G(add_belief(perc_rad938(low)))", ["add_belief(perc_rad938(low))", "add_belief(perc_rad938(high))"])
+!inspect_nuclear_plant938 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant938 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield939("G(add_belief(perc_rad939(low)))", ["add_belief(perc_rad939(low))", "add_belief(perc_rad939(high))"])
+!inspect_nuclear_plant939 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant939 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield940("G(add_belief(perc_rad940(low)))", ["add_belief(perc_rad940(low))", "add_belief(perc_rad940(high))"])
+!inspect_nuclear_plant940 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant940 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield941("G(add_belief(perc_rad941(low)))", ["add_belief(perc_rad941(low))", "add_belief(perc_rad941(high))"])
+!inspect_nuclear_plant941 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant941 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield942("G(add_belief(perc_rad942(low)))", ["add_belief(perc_rad942(low))", "add_belief(perc_rad942(high))"])
+!inspect_nuclear_plant942 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant942 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield943("G(add_belief(perc_rad943(low)))", ["add_belief(perc_rad943(low))", "add_belief(perc_rad943(high))"])
+!inspect_nuclear_plant943 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant943 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield944("G(add_belief(perc_rad944(low)))", ["add_belief(perc_rad944(low))", "add_belief(perc_rad944(high))"])
+!inspect_nuclear_plant944 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant944 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield945("G(add_belief(perc_rad945(low)))", ["add_belief(perc_rad945(low))", "add_belief(perc_rad945(high))"])
+!inspect_nuclear_plant945 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant945 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield946("G(add_belief(perc_rad946(low)))", ["add_belief(perc_rad946(low))", "add_belief(perc_rad946(high))"])
+!inspect_nuclear_plant946 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant946 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield947("G(add_belief(perc_rad947(low)))", ["add_belief(perc_rad947(low))", "add_belief(perc_rad947(high))"])
+!inspect_nuclear_plant947 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant947 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield948("G(add_belief(perc_rad948(low)))", ["add_belief(perc_rad948(low))", "add_belief(perc_rad948(high))"])
+!inspect_nuclear_plant948 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant948 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield949("G(add_belief(perc_rad949(low)))", ["add_belief(perc_rad949(low))", "add_belief(perc_rad949(high))"])
+!inspect_nuclear_plant949 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant949 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield950("G(add_belief(perc_rad950(low)))", ["add_belief(perc_rad950(low))", "add_belief(perc_rad950(high))"])
+!inspect_nuclear_plant950 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant950 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield951("G(add_belief(perc_rad951(low)))", ["add_belief(perc_rad951(low))", "add_belief(perc_rad951(high))"])
+!inspect_nuclear_plant951 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant951 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield952("G(add_belief(perc_rad952(low)))", ["add_belief(perc_rad952(low))", "add_belief(perc_rad952(high))"])
+!inspect_nuclear_plant952 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant952 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield953("G(add_belief(perc_rad953(low)))", ["add_belief(perc_rad953(low))", "add_belief(perc_rad953(high))"])
+!inspect_nuclear_plant953 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant953 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield954("G(add_belief(perc_rad954(low)))", ["add_belief(perc_rad954(low))", "add_belief(perc_rad954(high))"])
+!inspect_nuclear_plant954 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant954 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield955("G(add_belief(perc_rad955(low)))", ["add_belief(perc_rad955(low))", "add_belief(perc_rad955(high))"])
+!inspect_nuclear_plant955 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant955 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield956("G(add_belief(perc_rad956(low)))", ["add_belief(perc_rad956(low))", "add_belief(perc_rad956(high))"])
+!inspect_nuclear_plant956 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant956 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield957("G(add_belief(perc_rad957(low)))", ["add_belief(perc_rad957(low))", "add_belief(perc_rad957(high))"])
+!inspect_nuclear_plant957 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant957 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield958("G(add_belief(perc_rad958(low)))", ["add_belief(perc_rad958(low))", "add_belief(perc_rad958(high))"])
+!inspect_nuclear_plant958 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant958 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield959("G(add_belief(perc_rad959(low)))", ["add_belief(perc_rad959(low))", "add_belief(perc_rad959(high))"])
+!inspect_nuclear_plant959 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant959 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield960("G(add_belief(perc_rad960(low)))", ["add_belief(perc_rad960(low))", "add_belief(perc_rad960(high))"])
+!inspect_nuclear_plant960 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant960 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield961("G(add_belief(perc_rad961(low)))", ["add_belief(perc_rad961(low))", "add_belief(perc_rad961(high))"])
+!inspect_nuclear_plant961 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant961 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield962("G(add_belief(perc_rad962(low)))", ["add_belief(perc_rad962(low))", "add_belief(perc_rad962(high))"])
+!inspect_nuclear_plant962 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant962 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield963("G(add_belief(perc_rad963(low)))", ["add_belief(perc_rad963(low))", "add_belief(perc_rad963(high))"])
+!inspect_nuclear_plant963 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant963 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield964("G(add_belief(perc_rad964(low)))", ["add_belief(perc_rad964(low))", "add_belief(perc_rad964(high))"])
+!inspect_nuclear_plant964 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant964 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield965("G(add_belief(perc_rad965(low)))", ["add_belief(perc_rad965(low))", "add_belief(perc_rad965(high))"])
+!inspect_nuclear_plant965 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant965 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield966("G(add_belief(perc_rad966(low)))", ["add_belief(perc_rad966(low))", "add_belief(perc_rad966(high))"])
+!inspect_nuclear_plant966 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant966 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield967("G(add_belief(perc_rad967(low)))", ["add_belief(perc_rad967(low))", "add_belief(perc_rad967(high))"])
+!inspect_nuclear_plant967 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant967 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield968("G(add_belief(perc_rad968(low)))", ["add_belief(perc_rad968(low))", "add_belief(perc_rad968(high))"])
+!inspect_nuclear_plant968 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant968 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield969("G(add_belief(perc_rad969(low)))", ["add_belief(perc_rad969(low))", "add_belief(perc_rad969(high))"])
+!inspect_nuclear_plant969 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant969 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield970("G(add_belief(perc_rad970(low)))", ["add_belief(perc_rad970(low))", "add_belief(perc_rad970(high))"])
+!inspect_nuclear_plant970 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant970 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield971("G(add_belief(perc_rad971(low)))", ["add_belief(perc_rad971(low))", "add_belief(perc_rad971(high))"])
+!inspect_nuclear_plant971 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant971 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield972("G(add_belief(perc_rad972(low)))", ["add_belief(perc_rad972(low))", "add_belief(perc_rad972(high))"])
+!inspect_nuclear_plant972 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant972 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield973("G(add_belief(perc_rad973(low)))", ["add_belief(perc_rad973(low))", "add_belief(perc_rad973(high))"])
+!inspect_nuclear_plant973 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant973 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield974("G(add_belief(perc_rad974(low)))", ["add_belief(perc_rad974(low))", "add_belief(perc_rad974(high))"])
+!inspect_nuclear_plant974 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant974 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield975("G(add_belief(perc_rad975(low)))", ["add_belief(perc_rad975(low))", "add_belief(perc_rad975(high))"])
+!inspect_nuclear_plant975 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant975 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield976("G(add_belief(perc_rad976(low)))", ["add_belief(perc_rad976(low))", "add_belief(perc_rad976(high))"])
+!inspect_nuclear_plant976 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant976 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield977("G(add_belief(perc_rad977(low)))", ["add_belief(perc_rad977(low))", "add_belief(perc_rad977(high))"])
+!inspect_nuclear_plant977 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant977 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield978("G(add_belief(perc_rad978(low)))", ["add_belief(perc_rad978(low))", "add_belief(perc_rad978(high))"])
+!inspect_nuclear_plant978 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant978 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield979("G(add_belief(perc_rad979(low)))", ["add_belief(perc_rad979(low))", "add_belief(perc_rad979(high))"])
+!inspect_nuclear_plant979 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant979 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield980("G(add_belief(perc_rad980(low)))", ["add_belief(perc_rad980(low))", "add_belief(perc_rad980(high))"])
+!inspect_nuclear_plant980 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant980 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield981("G(add_belief(perc_rad981(low)))", ["add_belief(perc_rad981(low))", "add_belief(perc_rad981(high))"])
+!inspect_nuclear_plant981 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant981 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield982("G(add_belief(perc_rad982(low)))", ["add_belief(perc_rad982(low))", "add_belief(perc_rad982(high))"])
+!inspect_nuclear_plant982 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant982 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield983("G(add_belief(perc_rad983(low)))", ["add_belief(perc_rad983(low))", "add_belief(perc_rad983(high))"])
+!inspect_nuclear_plant983 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant983 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield984("G(add_belief(perc_rad984(low)))", ["add_belief(perc_rad984(low))", "add_belief(perc_rad984(high))"])
+!inspect_nuclear_plant984 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant984 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield985("G(add_belief(perc_rad985(low)))", ["add_belief(perc_rad985(low))", "add_belief(perc_rad985(high))"])
+!inspect_nuclear_plant985 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant985 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield986("G(add_belief(perc_rad986(low)))", ["add_belief(perc_rad986(low))", "add_belief(perc_rad986(high))"])
+!inspect_nuclear_plant986 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant986 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield987("G(add_belief(perc_rad987(low)))", ["add_belief(perc_rad987(low))", "add_belief(perc_rad987(high))"])
+!inspect_nuclear_plant987 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant987 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield988("G(add_belief(perc_rad988(low)))", ["add_belief(perc_rad988(low))", "add_belief(perc_rad988(high))"])
+!inspect_nuclear_plant988 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant988 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield989("G(add_belief(perc_rad989(low)))", ["add_belief(perc_rad989(low))", "add_belief(perc_rad989(high))"])
+!inspect_nuclear_plant989 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant989 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield990("G(add_belief(perc_rad990(low)))", ["add_belief(perc_rad990(low))", "add_belief(perc_rad990(high))"])
+!inspect_nuclear_plant990 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant990 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield991("G(add_belief(perc_rad991(low)))", ["add_belief(perc_rad991(low))", "add_belief(perc_rad991(high))"])
+!inspect_nuclear_plant991 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant991 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield992("G(add_belief(perc_rad992(low)))", ["add_belief(perc_rad992(low))", "add_belief(perc_rad992(high))"])
+!inspect_nuclear_plant992 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant992 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield993("G(add_belief(perc_rad993(low)))", ["add_belief(perc_rad993(low))", "add_belief(perc_rad993(high))"])
+!inspect_nuclear_plant993 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant993 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield994("G(add_belief(perc_rad994(low)))", ["add_belief(perc_rad994(low))", "add_belief(perc_rad994(high))"])
+!inspect_nuclear_plant994 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant994 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield995("G(add_belief(perc_rad995(low)))", ["add_belief(perc_rad995(low))", "add_belief(perc_rad995(high))"])
+!inspect_nuclear_plant995 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant995 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield996("G(add_belief(perc_rad996(low)))", ["add_belief(perc_rad996(low))", "add_belief(perc_rad996(high))"])
+!inspect_nuclear_plant996 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant996 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield997("G(add_belief(perc_rad997(low)))", ["add_belief(perc_rad997(low))", "add_belief(perc_rad997(high))"])
+!inspect_nuclear_plant997 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant997 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield998("G(add_belief(perc_rad998(low)))", ["add_belief(perc_rad998(low))", "add_belief(perc_rad998(high))"])
+!inspect_nuclear_plant998 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant998 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).


@shield999("G(add_belief(perc_rad999(low)))", ["add_belief(perc_rad999(low))", "add_belief(perc_rad999(high))"])
+!inspect_nuclear_plant999 : fast
<-
    !inspect(wp1);
    move_to(wp2, R2); -perc_rad(R2); +perc_rad(R2);
    .print("Detected level of radiation: ", R2);
    move_to(wp3, R3); -perc_rad(R3); +perc_rad(R3);
    .print("Detected level of radiation: ", R3);
    !inspect(wp3).
+!inspect_nuclear_plant999 : true
<-
    !inspect(wp1);
    move_to(wp4, R4); -perc_rad(R4); +perc_rad(R4);
    .print("Detected level of radiation: ", R4);
    move_to(wp5, R5); -perc_rad(R5); +perc_rad(R5);
    .print("Detected level of radiation: ", R5);
    !inspect(wp3).

